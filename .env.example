# GitHub Configuration (可选但推荐)
# ⚠️ 重要：GITHUB_TOKEN 仅用于本地开发和 GitHub Actions
# 
# 用途：
# 1. 提高 GitHub API 速率限制（无 token: 60次/小时，有 token: 5000次/小时）
# 2. 获取仓库信息用于项目展示页面
#
# 如何获取：
# 1. 访问 https://github.com/settings/tokens
# 2. 创建 Personal Access Token (classic)
# 3. 权限选择：public_repo (只读公开仓库)
# 4. 复制 token 到下方
#
# ⚠️ 安全提示：
# - 此文件 .env 已在 .gitignore 中，不会被提交到仓库
# - 请勿在 user.config.ts 中直接写入 token
# - GitHub Actions 会自动提供 GITHUB_TOKEN，无需手动设置
#
GITHUB_TOKEN=

# Site Configuration (部署时需要配置)
# 当前主题只支持根路径部署，例如自定义域名或 GitHub Pages 用户站点：
# SITE=https://yourusername.github.io
# BASE=/
SITE=https://yourusername.github.io
BASE=/
