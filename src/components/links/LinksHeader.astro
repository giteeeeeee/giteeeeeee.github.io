---
/**
 * LinksHeader.astro
 * Links page header and statistics
 */
import { useI18n } from '../../utils/i18n';

const { t } = useI18n();

interface Props {
  totalLinks: number;
  featuredLinks: number;
  friendsCount: number;
  sitesCount: number;
  socialCount: number;
}

const { totalLinks, featuredLinks, friendsCount, sitesCount, socialCount } = Astro.props;
---

<div class="links-header">
  <!-- 标题区 -->
  <div class="header-title">
    <h1 class="title">
      <span class="icon-wrapper">
        <i class="i-carbon:link"></i>
      </span>
      <span data-i18n="links.title">{t('links.title')}</span>
    </h1>
    <p class="subtitle" data-i18n="links.subtitle">
      {t('links.subtitle')}
    </p>
  </div>

  <!-- 统计信息 -->
  <div class="stats-grid">
    <div class="stat-card glass-card-stat">
      <div class="stat-icon">
        <i class="i-carbon:link"></i>
      </div>
      <div class="stat-info">
        <div class="stat-value">{totalLinks}</div>
        <div class="stat-label" data-i18n="links.totalLinks">{t('links.totalLinks')}</div>
      </div>
    </div>

    <div class="stat-card glass-card-stat featured">
      <div class="stat-icon">
        <i class="i-carbon:star-filled"></i>
      </div>
      <div class="stat-info">
        <div class="stat-value">{featuredLinks}</div>
        <div class="stat-label" data-i18n="links.featured">{t('links.featured')}</div>
      </div>
    </div>

    <div class="stat-card glass-card-stat">
      <div class="stat-icon">
        <i class="i-carbon:user-favorite"></i>
      </div>
      <div class="stat-info">
        <div class="stat-value">{friendsCount}</div>
        <div class="stat-label" data-i18n="links.friends">{t('links.friends')}</div>
      </div>
    </div>

    <div class="stat-card glass-card-stat">
      <div class="stat-icon">
        <i class="i-carbon:earth"></i>
      </div>
      <div class="stat-info">
        <div class="stat-value">{sitesCount}</div>
        <div class="stat-label" data-i18n="links.sites">{t('links.sites')}</div>
      </div>
    </div>

    <div class="stat-card glass-card-stat">
      <div class="stat-icon">
        <i class="i-carbon:share"></i>
      </div>
      <div class="stat-info">
        <div class="stat-value">{socialCount}</div>
        <div class="stat-label" data-i18n="links.social">{t('links.social')}</div>
      </div>
    </div>
  </div>
</div>

<script>
  import { initI18n } from '../../scripts/i18n-content-updater';
  initI18n();
</script>

<style>
  .links-header {
    margin-bottom: 2rem;
    padding-bottom: 1.5rem;
    border-bottom: 1px solid var(--md-sys-color-outline-variant);
  }

  .header-title {
    text-align: center;
    margin-bottom: 1.25rem;
  }

  .title {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 1.75rem;
    font-weight: 600;
    color: var(--md-sys-color-on-surface);
    margin: 0 0 0.5rem 0;
  }

  .title .icon-wrapper {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .title .icon-wrapper i {
    font-size: 1.5rem;
    color: var(--md-sys-color-primary);
  }

  .title > span:first-child {
    display: flex;
  }

  .subtitle {
    font-size: 0.875rem;
    color: var(--md-sys-color-on-surface-variant);
    margin: 0 0 0.75rem 0;
    opacity: 0.9;
  }

  /* Stats card grid */
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 0.875rem;
    max-width: 1000px;
    margin: 0 auto;
  }

  .stat-card {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .stat-card.featured .stat-icon {
    background: var(--md-sys-color-secondary-container);
    color: var(--md-sys-color-on-secondary-container);
  }

  .stat-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    border-radius: 10px;
    background: var(--md-sys-color-secondary-container);
    color: var(--md-sys-color-on-secondary-container);
    font-size: 1.25rem;
    flex-shrink: 0;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .stat-card:hover .stat-icon {
    transform: scale(1.1) rotate(5deg);
  }

  :root[data-theme="light"] .stat-card:hover .stat-icon,
  :root:not([data-theme]) .stat-card:hover .stat-icon {
    box-shadow: 0 4px 12px rgba(var(--md-sys-color-shadow-rgb), 0.15);
  }

  :root[data-theme="dark"] .stat-card:hover .stat-icon {
    box-shadow: 0 4px 12px rgba(var(--md-sys-color-shadow-rgb), 0.4);
  }

  .stat-card.featured .stat-icon {
    background: var(--md-sys-color-tertiary-container);
    color: var(--md-sys-color-on-tertiary-container);
  }

  .stat-info {
    flex: 1;
    min-width: 0;
  }

  .stat-value {
    font-size: 1.5rem;
    font-weight: 800;
    color: var(--md-sys-color-on-surface);
    line-height: 1;
    margin-bottom: 0.25rem;
    letter-spacing: -0.01em;
  }

  .stat-label {
    font-size: 0.75rem;
    color: var(--md-sys-color-on-surface-variant);
    font-weight: 600;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .stats-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  @media (max-width: 768px) {
    .links-header {
      padding: 1rem 0;
      margin-bottom: 2.5rem;
    }

    .header-title {
      margin-bottom: 1.5rem;
    }

    .title {
      font-size: 1.75rem;
      gap: 0.5rem;
      margin-bottom: 0.625rem;
    }

    .title i {
      font-size: 1.625rem;
    }

    .subtitle {
      font-size: 0.875rem;
    }

    .stats-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 0.75rem;
    }

    .stat-card {
      gap: 0.625rem;
    }

    .stat-icon {
      width: 36px;
      height: 36px;
      font-size: 1.125rem;
    }

    .stat-value {
      font-size: 1.25rem;
    }

    .stat-label {
      font-size: 0.6875rem;
    }
  }
</style>
