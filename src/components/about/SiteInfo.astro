---
/**
 * SiteInfo.astro
 * About this site information card
 */
import { useI18n } from '../../utils/i18n';

interface Props {
  site: {
    name: string;
    description: string;
    builtWith: string;
    since: string;
    stats: {
      posts: number;
      words: number;
      visitors: number;
    };
    techStack: Array<{
      name: string;
      description: string;
      url: string;
      icon: string;
    }>;
  };
}

const { site } = Astro.props;
const { t } = useI18n();
---

<section class="site-info-section">
  <div class="section-header">
    <div class="header-content">
      <i class="header-icon i-carbon:earth"></i>
      <h2 class="section-title" data-i18n="about.site.title">{t('about.site.title')}</h2>
    </div>
    <p class="section-description" data-i18n="about.site.description">{t('about.site.description')}</p>
  </div>

  <div class="site-info-grid">
    <!-- Basic information -->
    <div class="info-card main-info">
      <div class="site-header">
        <div class="site-icon">
          <i class="i-carbon:earth"></i>
        </div>
        <div>
          <h3 class="site-name">{site.name}</h3>
          <p class="site-since">Est. {site.since}</p>
        </div>
      </div>
      <p class="site-description">{site.description}</p>
      <p class="site-built">{site.builtWith}</p>
    </div>

    <!-- Statistics -->
    <div class="info-card stats-card">
      <h3 class="card-title">
        <i class="i-carbon:chart-line"></i>
        <span data-i18n="about.site.stats">{t('about.site.stats')}</span>
      </h3>
      <div class="stats-grid">
        <div class="stat-item">
          <div class="stat-value">{site.stats.posts}</div>
          <div class="stat-label" data-i18n="about.site.stats.posts">{t('about.site.stats.posts')}</div>
        </div>
        <div class="stat-item">
          <div class="stat-value">{Math.round(site.stats.words / 1000)}k</div>
          <div class="stat-label" data-i18n="about.site.stats.words">{t('about.site.stats.words')}</div>
        </div>
        <div class="stat-item">
          <div class="stat-value">{Math.round(site.stats.visitors / 1000)}k+</div>
          <div class="stat-label" data-i18n="about.site.stats.visitors">{t('about.site.stats.visitors')}</div>
        </div>
      </div>
    </div>

    <!-- Tech stack -->
    <div class="info-card tech-stack-card">
      <h3 class="card-title">
        <i class="i-carbon:code"></i>
        <span data-i18n="about.site.techStack">{t('about.site.techStack')}</span>
      </h3>
      <div class="tech-list">
        {site.techStack.map((tech) => (
          <a href={tech.url} target="_blank" rel="noopener noreferrer" class="tech-item">
            <i class={tech.icon}></i>
            <div class="tech-info">
              <div class="tech-name">{tech.name}</div>
              <div class="tech-desc">{tech.description}</div>
            </div>
            <i class="i-carbon:arrow-up-right tech-arrow"></i>
          </a>
        ))}
      </div>
    </div>
  </div>
</section>

<style>
  .site-info-section {
    margin-bottom: 3rem;
  }

  /* Unified header styling */
  .site-info-header {
    margin-bottom: 1.5rem;
    padding-bottom: 0.75rem;
    border-bottom: 2px solid var(--md-sys-color-tertiary);
  }

  .header-content {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 0.5rem;
  }

  .header-icon {
    font-size: 1.5rem;
    color: var(--md-sys-color-tertiary);
  }

  .section-title {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--md-sys-color-on-surface);
    margin: 0;
  }

  .section-description {
    font-size: 0.9375rem;
    color: var(--md-sys-color-on-surface-variant);
    margin: 0;
    line-height: 1.6;
  }

  .site-info-grid {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: 1rem;
  }

  @media (max-width: 968px) {
    .site-info-grid {
      grid-template-columns: 1fr;
    }
  }

  .info-card {
    padding: 1.5rem;
    background: var(--md-sys-color-surface-container-low);
    border: 1px solid var(--md-sys-color-outline-variant);
    border-radius: 16px;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .info-card:hover {
    background: var(--md-sys-color-surface-container);
    border-color: var(--md-sys-color-primary);
    transform: translateY(-2px);
    box-shadow: 
      0 4px 12px rgba(var(--md-sys-color-shadow-rgb), 0.05),
      0 2px 6px rgba(var(--md-sys-color-shadow-rgb), 0.03);
  }

  /* Main info card */
  .main-info {
    grid-row: span 2;
  }

  @media (max-width: 968px) {
    .main-info {
      grid-row: span 1;
    }
  }

  .site-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1rem;
  }

  .site-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 56px;
    height: 56px;
    background: linear-gradient(135deg, 
      var(--md-sys-color-primary-container) 0%, 
      var(--md-sys-color-secondary-container) 100%
    );
    border-radius: 14px;
    flex-shrink: 0;
  }

  .site-icon i {
    font-size: 28px;
    line-height: 1;
    color: var(--md-sys-color-on-primary-container);
    display: block;
  }

  .site-name {
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--md-sys-color-on-surface);
    margin: 0;
  }

  .site-since {
    font-size: 0.8125rem;
    color: var(--md-sys-color-on-surface-variant);
    margin: 0.25rem 0 0 0;
  }

  .site-description {
    font-size: 0.9375rem;
    color: var(--md-sys-color-on-surface);
    line-height: 1.6;
    margin: 0 0 1rem 0;
  }

  .site-built {
    font-size: 0.875rem;
    color: var(--md-sys-color-on-surface-variant);
    padding: 0.5rem 1rem;
    background: var(--md-sys-color-surface-container-highest);
    border-radius: 8px;
    margin: 0;
  }

  /* Stats card */
  .card-title {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 1rem;
    font-weight: 600;
    color: var(--md-sys-color-on-surface);
    margin: 0 0 1rem 0;
  }

  .card-title i {
    font-size: 18px;
  }

  .stats-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
  }

  .stat-item {
    text-align: center;
  }

  .stat-value {
    font-size: 1.75rem;
    font-weight: 700;
    color: var(--md-sys-color-primary);
    margin-bottom: 0.25rem;
  }

  .stat-label {
    font-size: 0.75rem;
    color: var(--md-sys-color-on-surface-variant);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  /* Tech stack card */
  .tech-stack-card {
    grid-column: 1 / -1;
  }

  .tech-list {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 0.875rem;
  }

  @media (max-width: 640px) {
    .tech-list {
      grid-template-columns: 1fr;
    }
  }

  .tech-item {
    display: flex;
    align-items: center;
    gap: 0.875rem;
    padding: 1rem;
    min-height: 72px;
    background: var(--md-sys-color-surface-container);
    border: 1px solid var(--md-sys-color-outline-variant);
    border-radius: 12px;
    text-decoration: none;
    color: inherit;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
  }

  .tech-item::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 3px;
    height: 100%;
    background: var(--md-sys-color-tertiary);
    opacity: 0;
    border-radius: 12px 0 0 12px;
    transition: opacity 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .tech-item:hover {
    background: var(--md-sys-color-surface-container-high);
    border-color: var(--md-sys-color-tertiary);
    transform: translateY(-2px);
    box-shadow: 
      0 4px 12px rgba(var(--md-sys-color-shadow-rgb), 0.06),
      0 2px 6px rgba(var(--md-sys-color-shadow-rgb), 0.03);
  }

  .tech-item:hover::before {
    opacity: 1;
  }

  .tech-item > i:first-child {
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    font-size: 22px;
    line-height: 1;
    color: var(--md-sys-color-on-tertiary-container);
    background: var(--md-sys-color-tertiary-container);
    border-radius: 10px;
    transition: all 0.3s ease;
  }

  .tech-item:hover > i:first-child {
    color: var(--md-sys-color-on-tertiary);
    background: var(--md-sys-color-tertiary);
    transform: scale(1.05);
  }

  .tech-info {
    flex: 1;
    min-width: 0;
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 0.25rem;
  }

  .tech-name {
    font-size: 0.9375rem;
    font-weight: 600;
    color: var(--md-sys-color-on-surface);
    margin: 0;
    line-height: 1.4;
    transition: color 0.3s ease;
  }

  .tech-item:hover .tech-name {
    color: var(--md-sys-color-tertiary);
  }

  .tech-desc {
    font-size: 0.8125rem;
    color: var(--md-sys-color-on-surface-variant);
    margin: 0;
    line-height: 1.4;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .tech-arrow {
    flex-shrink: 0;
    font-size: 16px;
    color: var(--md-sys-color-on-surface-variant);
    opacity: 0;
    transform: translate(-4px, 4px);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .tech-item:hover .tech-arrow {
    opacity: 1;
    transform: translate(0, 0);
  }
</style>
