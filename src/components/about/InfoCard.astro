---
/**
 * InfoCard.astro
 * General info card component - supports multiple styles and layouts
 */
interface Props {
  title: string;
  description?: string;
  icon?: string;
  variant?: 'default' | 'highlight' | 'compact' | 'wide';
  class?: string;
}

const { 
  title, 
  description, 
  icon,
  variant = 'default',
  class: className = '',
} = Astro.props;
---

<div class={`info-card ${variant} ${className}`}>
  {icon && (
    <div class="card-icon">
      <i class={icon}></i>
    </div>
  )}
  <div class="card-content">
    <h3 class="card-title">{title}</h3>
    {description && <p class="card-description">{description}</p>}
    <div class="card-body">
      <slot />
    </div>
  </div>
</div>

<style>
  .info-card {
    position: relative;
    padding: 1.5rem;
    background: var(--md-sys-color-surface-container-low);
    border: 1px solid var(--md-sys-color-outline-variant);
    border-radius: 16px;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .info-card:hover {
    background: var(--md-sys-color-surface-container);
    border-color: var(--md-sys-color-primary);
    transform: translateY(-2px);
    box-shadow: 
      0 4px 12px rgba(var(--md-sys-color-shadow-rgb), 0.05),
      0 2px 6px rgba(var(--md-sys-color-shadow-rgb), 0.03);
  }

  /* Highlight style */
  .info-card.highlight {
    background: linear-gradient(135deg, 
      var(--md-sys-color-primary-container) 0%, 
      var(--md-sys-color-secondary-container) 100%
    );
    border-color: var(--md-sys-color-primary);
  }

  .info-card.highlight .card-title {
    color: var(--md-sys-color-on-primary-container);
  }

  /* Compact style */
  .info-card.compact {
    padding: 1rem;
  }

  .info-card.compact .card-title {
    font-size: 0.9375rem;
  }

  /* Wide card style */
  .info-card.wide {
    grid-column: span 2;
  }

  @media (max-width: 768px) {
    .info-card.wide {
      grid-column: span 1;
    }
  }

  .card-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 48px;
    height: 48px;
    margin-bottom: 1rem;
    background: var(--md-sys-color-primary-container);
    border-radius: 12px;
  }

  .card-icon i {
    font-size: 24px;
    color: var(--md-sys-color-on-primary-container);
  }

  .card-content {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .card-title {
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--md-sys-color-on-surface);
    margin: 0;
  }

  .card-description {
    font-size: 0.875rem;
    color: var(--md-sys-color-on-surface-variant);
    margin: 0;
    line-height: 1.6;
  }

  .card-body {
    margin-top: 0.5rem;
  }
</style>
