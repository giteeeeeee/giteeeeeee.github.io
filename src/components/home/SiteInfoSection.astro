---
/**
 * SiteInfoSection Component
 * Site information section for homepage
 * Displays tech stack, statistics, and footer
 */
import { getAboutConfig } from '../../data/site.config';
import { getArchiveStats } from '../../utils/blog';
import { getPlogGallery } from '../../utils/plog';
import { useI18n } from '../../utils/i18n';
import Footer from '../common/Footer.astro';

const { currentLang } = useI18n();
const aboutConfig = getAboutConfig();
const stats = await getArchiveStats();
const gallery = await getPlogGallery();
const isZh = currentLang === 'zh';
const labels = isZh
  ? {
      since: '建站时间',
      latest: '最近更新',
      contentMap: '内容地图',
      contentMapSubtitle: '这个站点主要存放这些内容',
      blog: '文章',
      blogCaption: '技术笔记 / 长文整理',
      gallery: '相册',
      galleryCaption: '照片合集 / 旅行影像',
      projects: '项目',
      projectsCaption: 'GitHub 实践与作品',
      guestbook: '留言',
      guestbookCaption: '评论与来访记录',
      build: '构建方式',
      principles: '站点原则',
      staticFirst: '静态优先',
      staticFirstDesc: '页面预渲染，访问路径更短',
      contentFirst: '内容优先',
      contentFirstDesc: '博客与相册都由内容文件驱动',
      portable: '易于迁移',
      portableDesc: '配置、内容和媒体分层管理',
      photos: '张照片',
      albums: '个合集',
      open: '开放',
      unknown: '持续更新',
    }
  : {
      since: 'Since',
      latest: 'Latest update',
      contentMap: 'Content Map',
      contentMapSubtitle: 'Main spaces this site keeps organized',
      blog: 'Posts',
      blogCaption: 'Notes and long-form writing',
      gallery: 'Gallery',
      galleryCaption: 'Photo albums and travel images',
      projects: 'Projects',
      projectsCaption: 'GitHub work and experiments',
      guestbook: 'Guestbook',
      guestbookCaption: 'Comments and visitor notes',
      build: 'Build',
      principles: 'Site Principles',
      staticFirst: 'Static first',
      staticFirstDesc: 'Pre-rendered pages keep visits short',
      contentFirst: 'Content first',
      contentFirstDesc: 'Posts and albums are driven by content files',
      portable: 'Portable',
      portableDesc: 'Config, content, and media stay separated',
      photos: 'photos',
      albums: 'albums',
      open: 'Open',
      unknown: 'Updating',
    };

const formatDate = (date?: Date | string | null) => {
  if (!date) return labels.unknown;
  return new Date(date).toLocaleDateString(isZh ? 'zh-CN' : 'en', {
    year: 'numeric',
    month: 'short',
    day: 'numeric',
  });
};

const contentCards = [
  {
    href: '/blog',
    icon: 'i-carbon:document',
    value: stats.totalPosts,
    label: labels.blog,
    caption: labels.blogCaption,
  },
  {
    href: '/gallery',
    icon: 'i-carbon:image',
    value: gallery.albums.length,
    label: labels.gallery,
    caption: `${gallery.photos.length} ${labels.photos}`,
  },
  {
    href: '/projects',
    icon: 'i-carbon:code',
    value: aboutConfig.site.techStack.length,
    label: labels.projects,
    caption: labels.projectsCaption,
  },
  {
    href: '/guestbook',
    icon: 'i-carbon:chat',
    value: labels.open,
    label: labels.guestbook,
    caption: labels.guestbookCaption,
  },
];

const siteFacts = [
  {
    label: labels.since,
    value: aboutConfig.site.since,
  },
  {
    label: labels.latest,
    value: formatDate(stats.latestPostDate),
  },
  {
    label: labels.gallery,
    value: `${gallery.albums.length} ${labels.albums}`,
  },
];

const buildNotes = [
  {
    icon: 'i-carbon:flash-filled',
    title: labels.staticFirst,
    description: labels.staticFirstDesc,
  },
  {
    icon: 'i-carbon:folder-details',
    title: labels.contentFirst,
    description: labels.contentFirstDesc,
  },
  {
    icon: 'i-carbon:ibm-cloud-pak-integration',
    title: labels.portable,
    description: labels.portableDesc,
  },
];

const statCards = [
  {
    icon: 'i-carbon:document',
    value: stats.totalPosts,
    label: 'site.stats.articles',
    gradient: 'linear-gradient(135deg, rgba(var(--md-sys-color-primary-rgb), 0.18), rgba(var(--md-sys-color-tertiary-rgb), 0.14))',
    color: 'var(--md-sys-color-primary)',
  },
  {
    icon: 'i-carbon:tag',
    value: stats.totalTags,
    label: 'site.stats.tags',
    gradient: 'linear-gradient(135deg, rgba(var(--md-sys-color-tertiary-rgb), 0.18), rgba(var(--md-sys-color-secondary-rgb), 0.14))',
    color: 'var(--md-sys-color-tertiary)',
  },
  {
    icon: 'i-carbon:text-font',
    value: `${Math.round(stats.totalWords / 1000)}K`,
    label: 'site.stats.words',
    gradient: 'linear-gradient(135deg, rgba(var(--reay-color-success-rgb), 0.18), rgba(var(--md-sys-color-secondary-rgb), 0.14))',
    color: 'var(--reay-color-success)',
  },
  {
    icon: 'i-carbon:calendar',
    value: stats.postsThisYear,
    label: 'site.stats.thisYear',
    gradient: 'linear-gradient(135deg, rgba(var(--reay-color-warning-rgb), 0.18), rgba(var(--md-sys-color-tertiary-rgb), 0.14))',
    color: 'var(--reay-color-warning)',
  },
];
---

<div class="home-section-frame site-info-frame h-full box-border px-4 py-6">
  <div class="site-info-section home-section-shell home-section-accent-primary max-w-6xl mx-auto w-full">
    <!-- Section Header -->
    <div class="section-header home-section-heading mb-6 relative">
      <div class="absolute inset-0 -z-10 opacity-30">
        <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-64 h-64 bg-gradient-to-br from-[var(--md-sys-color-primary)]/10 to-[var(--md-sys-color-tertiary)]/10 rounded-full blur-3xl"></div>
      </div>
      
      <span class="section-logo icon-wrapper inline-flex items-center justify-center bg-gradient-to-br from-[var(--md-sys-color-primary-container)] to-[var(--md-sys-color-tertiary-container)] rounded-xl" aria-hidden="true">
        <i class="i-carbon:information text-xl text-[var(--md-sys-color-primary)]"></i>
      </span>
      <h2 class="section-title inline-flex items-center justify-start gap-2 text-2xl md:text-3xl font-700 text-[var(--md-sys-color-on-surface)] mb-2">
        <span class="section-title-text bg-gradient-to-r from-[var(--md-sys-color-primary)] to-[var(--md-sys-color-tertiary)] bg-clip-text text-transparent" data-i18n="site.title">
          关于本站
        </span>
      </h2>
      <p class="section-subtitle text-xs md:text-sm text-[var(--md-sys-color-on-surface-variant)] font-500">
        <span class="section-subtitle-text" data-i18n="site.subtitle">{aboutConfig.site.description}</span>
      </p>
      
      <!-- 装饰线条 -->
      <div class="section-divider flex items-center justify-start gap-2 mt-3">
        <div class="w-12 h-0.5 bg-gradient-to-r from-transparent to-[var(--md-sys-color-primary)]/50"></div>
        <div class="w-2 h-2 rounded-full bg-[var(--md-sys-color-primary)]"></div>
        <div class="w-12 h-0.5 bg-gradient-to-l from-transparent to-[var(--md-sys-color-primary)]/50"></div>
      </div>
    </div>

    <!-- 主要内容区 -->
    <div class="site-content home-section-content">
      <div class="site-overview-card animate-fade-in">
        <div class="site-overview-icon">
          <i class="i-carbon:earth-filled"></i>
        </div>
        <div class="site-overview-body">
          <div class="site-overview-meta">
            <span>{labels.build}</span>
            <span data-i18n="site.tech.title">技术栈</span>
          </div>
          <h3>{aboutConfig.site.name}</h3>
          <p data-i18n="site.subtitle">{aboutConfig.site.description}</p>
        </div>
        <div class="site-facts">
          {siteFacts.map((fact) => (
            <div class="site-fact">
              <span>{fact.label}</span>
              <strong>{fact.value}</strong>
            </div>
          ))}
        </div>
      </div>

      <div class="content-map-section animate-fade-in" style="animation-delay: 180ms;">
        <div class="content-map-header">
          <div>
            <span>{labels.contentMap}</span>
            <h3>{labels.contentMapSubtitle}</h3>
          </div>
          <i class="i-carbon:map"></i>
        </div>
        <div class="content-map-grid">
          {contentCards.map((card, index) => (
            <a
              href={card.href}
              class="content-map-card"
              style={`animation-delay: ${index * 80 + 260}ms;`}
            >
              <span class="content-map-icon">
                <i class={card.icon}></i>
              </span>
              <span class="content-map-value">{card.value}</span>
              <strong>{card.label}</strong>
              <p>{card.caption}</p>
            </a>
          ))}
        </div>
      </div>

      <!-- 网站统计 - 美化版 -->
      <div class="stats-grid grid gap-3 grid-cols-2 lg:grid-cols-4">
        {statCards.map((stat, index) => (
          <div 
            class="stat-card group relative animate-scale-in"
            style={`animation-delay: ${index * 100}ms;`}
          >
            <!-- 装饰性渐变背景 -->
            <div class="absolute inset-0 opacity-0 group-hover:opacity-100 transition-opacity duration-500 rounded-xl" style={`background: ${stat.gradient};`}></div>
            
            <!-- 右上角光晕 -->
            <div class="absolute -top-6 -right-6 w-20 h-20 bg-gradient-to-br from-[var(--md-sys-color-primary)]/10 to-[var(--md-sys-color-tertiary)]/10 rounded-full blur-2xl group-hover:scale-150 transition-transform duration-500"></div>
            
            <div class="relative z-10 flex items-center gap-3 p-3 md:p-4 bg-gradient-to-br from-[var(--md-sys-color-surface-container-low)]/80 to-[var(--md-sys-color-surface-container)]/80 backdrop-blur-md border border-[var(--md-sys-color-outline-variant)]/50 rounded-xl transition-all duration-300 group-hover:border-[var(--md-sys-color-primary)] group-hover:-translate-y-1 group-hover:shadow-[var(--reay-shadow-panel)]">
              <div class={`stat-icon w-10 h-10 md:w-12 md:h-12 flex items-center justify-center bg-gradient-to-br from-[var(--md-sys-color-primary-container)] to-[var(--md-sys-color-tertiary-container)] rounded-lg flex-shrink-0 group-hover:scale-110 transition-transform duration-300`}>
                <i class={`${stat.icon} text-lg md:text-xl group-hover:animate-pulse`} style={`color: ${stat.color};`}></i>
              </div>
              <div class="stat-info flex-1">
                <div class="stat-value text-xl md:text-2xl font-700 text-[var(--md-sys-color-on-surface)] leading-none mb-1 group-hover:scale-105 transition-transform duration-300">{stat.value}</div>
                <div class="stat-label text-xs md:text-sm text-[var(--md-sys-color-on-surface-variant)] font-500" data-i18n={stat.label}></div>
              </div>
            </div>
          </div>
        ))}
      </div>

      <!-- 技术栈 - 美化版 -->
      <div class="tech-stack-section relative p-4 md:p-5 bg-gradient-to-br from-[var(--md-sys-color-surface-container-low)]/80 to-[var(--md-sys-color-surface-container)]/80 backdrop-blur-md border border-[var(--md-sys-color-outline-variant)]/50 rounded-xl overflow-hidden animate-fade-in" style="animation-delay: 400ms;">
        <!-- 装饰性背景 -->
        <div class="absolute top-0 right-0 w-40 h-40 bg-gradient-to-br from-[var(--md-sys-color-primary)]/5 to-transparent rounded-full blur-3xl"></div>
        
        <div class="relative z-10">
          <div class="tech-header flex items-center justify-between mb-3">
            <div class="flex items-center gap-2">
              <div class="icon-wrapper inline-flex items-center justify-center w-8 h-8 bg-gradient-to-br from-[var(--md-sys-color-primary-container)] to-[var(--md-sys-color-tertiary-container)] rounded-lg">
                <i class="i-carbon:code text-base text-[var(--md-sys-color-primary)]"></i>
              </div>
              <h3 class="text-lg md:text-xl font-700 text-[var(--md-sys-color-on-surface)]" data-i18n="site.tech.title">技术栈</h3>
            </div>
            <span class="tech-badge inline-flex items-center gap-1 px-2 py-1 text-xs font-600 bg-gradient-to-r from-[var(--md-sys-color-primary-container)]/60 to-[var(--md-sys-color-tertiary-container)]/60 text-[var(--md-sys-color-on-primary-container)] rounded-full">
              <i class="i-carbon:checkmark-filled text-xs"></i>
              <span data-i18n="site.tech.subtitle">现代化</span>
            </span>
          </div>
          <p class="tech-description text-xs md:text-sm text-[var(--md-sys-color-on-surface-variant)] mb-4 leading-relaxed" data-i18n="site.tech.description">{aboutConfig.site.builtWith}</p>
          
          <div class="tech-grid grid gap-2 md:gap-3 grid-cols-1 md:grid-cols-2 lg:grid-cols-3">
            {aboutConfig.site.techStack.map((tech, index) => (
              <a 
                href={tech.url} 
                target="_blank" 
                rel="noopener noreferrer"
                class="tech-card group/tech relative animate-scale-in"
                style={`animation-delay: ${index * 80 + 500}ms;`}
              >
                <!-- 装饰性渐变背景 -->
                <div class="absolute inset-0 bg-gradient-to-br from-[var(--md-sys-color-primary)]/5 to-transparent opacity-0 group-hover/tech:opacity-100 transition-opacity duration-300 rounded-lg"></div>
                
                <div class="relative z-10 flex items-center gap-3 p-3 bg-[var(--md-sys-color-surface-container)]/70 backdrop-blur-sm border border-[var(--md-sys-color-outline-variant)]/50 rounded-lg transition-all duration-300 group-hover/tech:bg-[var(--md-sys-color-surface-container-high)]/70 group-hover/tech:border-[var(--md-sys-color-primary)] group-hover/tech:-translate-y-1 group-hover/tech:shadow-[var(--reay-shadow-panel)] no-underline">
                  <i class={`${tech.icon} text-2xl text-[var(--md-sys-color-primary)] flex-shrink-0 group-hover/tech:scale-110 transition-transform duration-300`}></i>
                  <div class="tech-info flex-1 min-w-0">
                    <h4 class="text-sm font-600 text-[var(--md-sys-color-on-surface)] group-hover/tech:text-[var(--md-sys-color-primary)] transition-colors leading-snug mb-0.5">{tech.name}</h4>
                    <p class="text-xs text-[var(--md-sys-color-on-surface-variant)]/80 leading-snug m-0 line-clamp-1">{tech.description}</p>
                  </div>
                  <i class="i-carbon:arrow-up-right text-sm text-[var(--md-sys-color-on-surface-variant)] opacity-0 group-hover/tech:opacity-100 transition-all duration-300 transform translate-x-1 -translate-y-1 group-hover/tech:translate-x-0 group-hover/tech:translate-y-0"></i>
                </div>
              </a>
            ))}
          </div>

          <div class="build-notes">
            {buildNotes.map((note) => (
              <div class="build-note">
                <span>
                  <i class={note.icon}></i>
                </span>
                <div>
                  <strong>{note.title}</strong>
                  <p>{note.description}</p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Footer 固定在底部 - 使用 mt-auto 自动推到底部 -->
  <div class="site-footer max-w-6xl mx-auto w-full">
    <Footer />
  </div>
</div>

<style>
  /* Text truncation utility */
  .line-clamp-1 {
    display: -webkit-box;
    -webkit-line-clamp: 1;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  /* Animation definitions */
  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes scale-in {
    from {
      opacity: 0;
      transform: scale(0.9);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  .animate-fade-in {
    opacity: 0;
    animation: fade-in 0.6s cubic-bezier(0.4, 0, 0.2, 1) forwards;
  }

  .animate-scale-in {
    opacity: 0;
    animation: scale-in 0.4s cubic-bezier(0.4, 0, 0.2, 1) forwards;
  }

  .site-info-frame {
    display: flex;
    flex-direction: column;
    overflow-y: auto;
  }

  .site-info-frame > .site-footer {
    flex: 0 0 auto;
    margin-top: auto;
  }

  .site-content {
    display: grid;
    gap: 1rem;
  }

  .site-overview-card {
    position: relative;
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    align-items: center;
    gap: 0.9rem;
    overflow: hidden;
    padding: 1rem;
    border: 1px solid rgba(var(--md-sys-color-outline-variant-rgb), 0.42);
    border-radius: var(--reay-radius-lg, 1.35rem);
    background:
      radial-gradient(circle at 100% 0%, rgba(var(--md-sys-color-primary-rgb), 0.16), transparent 42%),
      linear-gradient(135deg, rgba(var(--md-sys-color-surface-container-low-rgb), 0.82), rgba(var(--md-sys-color-surface-container-rgb), 0.58));
    box-shadow: var(--reay-surface-shadow);
    backdrop-filter: blur(var(--reay-glass-blur, 14px)) saturate(150%);
    -webkit-backdrop-filter: blur(var(--reay-glass-blur, 14px)) saturate(150%);
  }

  .site-overview-card::after {
    content: '';
    position: absolute;
    inset: auto 0 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(var(--md-sys-color-primary-rgb), 0.56), transparent);
  }

  .site-overview-icon {
    position: relative;
    z-index: 1;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 3.2rem;
    height: 3.2rem;
    border: 1px solid rgba(var(--md-sys-color-primary-rgb), 0.28);
    border-radius: 1.05rem;
    color: var(--md-sys-color-primary);
    background: linear-gradient(135deg, rgba(var(--md-sys-color-primary-container-rgb), 0.74), rgba(var(--md-sys-color-tertiary-container-rgb), 0.48));
    box-shadow: 0 16px 34px -24px rgba(var(--md-sys-color-primary-rgb), 0.84);
  }

  .site-overview-icon i {
    font-size: 1.45rem;
  }

  .site-overview-body {
    position: relative;
    z-index: 1;
    min-width: 0;
  }

  .site-overview-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 0.38rem;
    margin-bottom: 0.35rem;
  }

  .site-overview-meta span {
    display: inline-flex;
    align-items: center;
    min-height: 1.4rem;
    padding: 0.15rem 0.48rem;
    border: 1px solid rgba(var(--md-sys-color-outline-variant-rgb), 0.34);
    border-radius: 999px;
    color: var(--md-sys-color-primary);
    background: rgba(var(--md-sys-color-primary-container-rgb), 0.26);
    font-size: 0.68rem;
    font-weight: 750;
  }

  .site-overview-body h3 {
    margin: 0;
    overflow: hidden;
    color: var(--md-sys-color-on-surface);
    font-size: clamp(1rem, 1.7vw, 1.28rem);
    font-weight: 800;
    line-height: 1.18;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .site-overview-body p {
    display: -webkit-box;
    margin: 0.38rem 0 0;
    overflow: hidden;
    color: var(--md-sys-color-on-surface-variant);
    font-size: 0.78rem;
    line-height: 1.55;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 2;
  }

  .site-facts {
    grid-column: 1 / -1;
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0.5rem;
  }

  .site-fact {
    min-width: 0;
    padding: 0.58rem 0.65rem;
    border: 1px solid rgba(var(--md-sys-color-outline-variant-rgb), 0.34);
    border-radius: var(--reay-radius-md, 1rem);
    background: rgba(var(--md-sys-color-surface-container-high-rgb), 0.34);
  }

  .site-fact span,
  .content-map-header span {
    display: block;
    color: var(--md-sys-color-primary);
    font-size: 0.64rem;
    font-weight: 820;
    letter-spacing: 0;
    text-transform: uppercase;
  }

  .site-fact strong {
    display: block;
    margin-top: 0.18rem;
    overflow: hidden;
    color: var(--md-sys-color-on-surface);
    font-size: 0.76rem;
    line-height: 1.22;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .content-map-section,
  .build-note {
    border: 1px solid rgba(var(--md-sys-color-outline-variant-rgb), 0.42);
    background:
      radial-gradient(circle at 100% 0%, rgba(var(--md-sys-color-tertiary-rgb), 0.12), transparent 42%),
      linear-gradient(135deg, rgba(var(--md-sys-color-surface-container-low-rgb), 0.76), rgba(var(--md-sys-color-surface-container-rgb), 0.58));
    backdrop-filter: blur(var(--reay-glass-blur, 14px)) saturate(150%);
    -webkit-backdrop-filter: blur(var(--reay-glass-blur, 14px)) saturate(150%);
  }

  .content-map-section {
    display: grid;
    gap: 0.75rem;
    overflow: hidden;
    padding: 1rem;
    border-radius: var(--reay-radius-lg, 1.35rem);
    box-shadow: var(--reay-surface-shadow);
  }

  .content-map-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.8rem;
  }

  .content-map-header h3 {
    margin: 0.18rem 0 0;
    color: var(--md-sys-color-on-surface);
    font-size: clamp(0.95rem, 1.45vw, 1.14rem);
    font-weight: 820;
    line-height: 1.2;
  }

  .content-map-header > i {
    display: inline-flex;
    flex: 0 0 auto;
    color: var(--md-sys-color-primary);
    font-size: 1.35rem;
  }

  .content-map-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.55rem;
  }

  .content-map-card {
    position: relative;
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    grid-template-areas:
      'icon value'
      'icon title'
      'copy copy';
    align-items: center;
    gap: 0.12rem 0.58rem;
    min-width: 0;
    padding: 0.7rem;
    overflow: hidden;
    border: 1px solid rgba(var(--md-sys-color-outline-variant-rgb), 0.34);
    border-radius: var(--reay-radius-md, 1rem);
    color: inherit;
    background: rgba(var(--md-sys-color-surface-container-high-rgb), 0.34);
    text-decoration: none;
    transition: transform 0.22s var(--reay-ease-out), border-color 0.22s ease, background 0.22s ease;
  }

  .content-map-card:hover {
    border-color: rgba(var(--md-sys-color-primary-rgb), 0.5);
    background: rgba(var(--md-sys-color-primary-container-rgb), 0.24);
    transform: translateY(-2px);
  }

  .content-map-icon {
    grid-area: icon;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2rem;
    height: 2rem;
    border-radius: 0.78rem;
    color: var(--md-sys-color-primary);
    background: rgba(var(--md-sys-color-primary-container-rgb), 0.52);
  }

  .content-map-value {
    grid-area: value;
    color: var(--md-sys-color-primary);
    font-size: 1rem;
    font-weight: 850;
    line-height: 1;
  }

  .content-map-card strong {
    grid-area: title;
    color: var(--md-sys-color-on-surface);
    font-size: 0.76rem;
    line-height: 1.2;
  }

  .content-map-card p {
    grid-area: copy;
    display: -webkit-box;
    margin: 0.35rem 0 0;
    overflow: hidden;
    color: var(--md-sys-color-on-surface-variant);
    font-size: 0.66rem;
    line-height: 1.35;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 2;
  }

  .build-notes {
    display: grid;
    gap: 0.48rem;
    margin-top: 0.75rem;
  }

  .build-note {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    gap: 0.55rem;
    align-items: center;
    min-width: 0;
    padding: 0.58rem 0.65rem;
    border-radius: var(--reay-radius-md, 1rem);
  }

  .build-note span {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.8rem;
    height: 1.8rem;
    border-radius: 0.68rem;
    color: var(--md-sys-color-tertiary);
    background: rgba(var(--md-sys-color-tertiary-container-rgb), 0.5);
  }

  .build-note strong {
    display: block;
    color: var(--md-sys-color-on-surface);
    font-size: 0.74rem;
    line-height: 1.2;
  }

  .build-note p {
    margin: 0.18rem 0 0;
    color: var(--md-sys-color-on-surface-variant);
    font-size: 0.64rem;
    line-height: 1.35;
  }

  /* Icon wrapper pulsing glow animation */
  .section-header .icon-wrapper {
    animation: pulse-glow 2s ease-in-out infinite;
  }
  
  @keyframes pulse-glow {
    0%, 100% {
      box-shadow: 0 0 0 0 var(--md-sys-color-primary);
      opacity: 1;
    }
    50% {
      box-shadow: 0 0 20px 5px var(--md-sys-color-primary);
      opacity: 0.8;
    }
  }

  @media (min-width: 1025px) {
    .site-info-frame {
      display: grid;
      grid-template-rows: minmax(0, 1fr) auto;
      align-items: stretch;
      gap: 0.55rem;
      overflow: hidden;
      padding-block: clamp(0.45rem, 1.2vh, 0.85rem);
    }

    :global(body.layout-home) .site-info-frame .site-info-section.home-section-shell {
      min-height: 0;
      height: 100%;
      align-self: stretch;
      padding-block: clamp(0.25rem, 1vh, 0.65rem);
    }

    .site-info-frame .site-content {
      display: grid;
      grid-template-columns: minmax(16rem, 0.78fr) minmax(0, 1.22fr);
      grid-template-rows: auto minmax(0, 1fr);
      align-content: center;
      gap: 0.65rem;
      min-height: 0;
    }

    .site-info-frame .site-overview-card {
      grid-column: 1;
      grid-row: 1;
      padding: 0.72rem;
      gap: 0.65rem;
      border-radius: 0.95rem;
    }

    .site-info-frame .site-facts {
      gap: 0.38rem;
    }

    .site-info-frame .site-fact {
      padding: 0.42rem 0.48rem;
      border-radius: 0.75rem;
    }

    .site-info-frame .site-fact span {
      font-size: 0.56rem;
    }

    .site-info-frame .site-fact strong {
      font-size: 0.64rem;
    }

    .site-info-frame .site-overview-icon {
      width: 2.45rem;
      height: 2.45rem;
      border-radius: 0.82rem;
    }

    .site-info-frame .site-overview-icon i {
      font-size: 1.16rem;
    }

    .site-info-frame .site-overview-meta {
      margin-bottom: 0.25rem;
      gap: 0.28rem;
    }

    .site-info-frame .site-overview-meta span {
      min-height: 1.18rem;
      padding: 0.08rem 0.38rem;
      font-size: 0.62rem;
    }

    .site-info-frame .site-overview-body h3 {
      font-size: clamp(0.9rem, 1.35vw, 1.05rem);
    }

    .site-info-frame .site-overview-body p {
      margin-top: 0.28rem;
      font-size: 0.68rem;
      line-height: 1.42;
      -webkit-line-clamp: 2;
    }

    .site-info-frame .stats-grid {
      grid-column: 1;
      grid-row: 2;
      gap: 0.55rem;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      align-self: start;
    }

    .site-info-frame .content-map-section {
      grid-column: 2;
      grid-row: 1;
      gap: 0.55rem;
      padding: 0.7rem 0.78rem;
      border-radius: 0.95rem;
    }

    .site-info-frame .content-map-header h3 {
      font-size: clamp(0.85rem, 1.2vw, 0.98rem);
    }

    .site-info-frame .content-map-grid {
      gap: 0.42rem;
    }

    .site-info-frame .content-map-card {
      padding: 0.52rem;
      border-radius: 0.78rem;
    }

    .site-info-frame .content-map-icon {
      width: 1.7rem;
      height: 1.7rem;
      border-radius: 0.66rem;
    }

    .site-info-frame .content-map-value {
      font-size: 0.88rem;
    }

    .site-info-frame .content-map-card strong {
      font-size: 0.68rem;
    }

    .site-info-frame .content-map-card p {
      margin-top: 0.24rem;
      font-size: 0.6rem;
      -webkit-line-clamp: 1;
    }

    .site-info-frame .stat-card > .relative {
      gap: 0.55rem;
      padding: 0.65rem 0.72rem;
      border-radius: 0.95rem;
    }

    .site-info-frame .stat-icon {
      width: 2.15rem;
      height: 2.15rem;
      border-radius: 0.72rem;
    }

    .site-info-frame .stat-value {
      font-size: clamp(1rem, 1.8vw, 1.25rem);
    }

    .site-info-frame .stat-label {
      font-size: 0.72rem;
    }

    .site-info-frame .tech-stack-section {
      grid-column: 2;
      grid-row: 2;
      align-self: stretch;
      padding: 0.72rem 0.82rem;
      border-radius: 0.95rem;
    }

    .site-info-frame .tech-header {
      margin-bottom: 0.45rem;
    }

    .site-info-frame .tech-description {
      margin-bottom: 0.55rem;
      font-size: 0.76rem;
      line-height: 1.45;
    }

    .site-info-frame .tech-grid {
      gap: 0.5rem;
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .site-info-frame .tech-card > .relative {
      gap: 0.55rem;
      padding: 0.55rem 0.62rem;
      border-radius: 0.78rem;
    }

    .site-info-frame .tech-card i:first-child {
      font-size: 1.28rem;
    }

    .site-info-frame .tech-info h4 {
      font-size: 0.8rem;
    }

    .site-info-frame .tech-info p {
      font-size: 0.68rem;
    }

    .site-info-frame .build-notes {
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 0.42rem;
      margin-top: 0.55rem;
    }

    .site-info-frame .build-note {
      grid-template-columns: 1fr;
      align-content: start;
      gap: 0.35rem;
      padding: 0.5rem;
    }

    .site-info-frame .build-note span {
      width: 1.55rem;
      height: 1.55rem;
      border-radius: 0.58rem;
    }

    .site-info-frame .build-note strong {
      font-size: 0.66rem;
    }

    .site-info-frame .build-note p {
      display: -webkit-box;
      overflow: hidden;
      font-size: 0.58rem;
      -webkit-box-orient: vertical;
      -webkit-line-clamp: 2;
    }

    .site-info-frame > .site-footer {
      margin-top: 0 !important;
      min-height: 0;
    }

    :global(body.layout-home .site-info-frame footer.site-footer .footer-inner) {
      padding: 0.5rem 0.75rem 0.45rem;
    }

    :global(body.layout-home .site-info-frame footer.site-footer .footer-main) {
      gap: 0.65rem;
    }

    :global(body.layout-home .site-info-frame footer.site-footer .footer-avatar) {
      width: 1.7rem;
      height: 1.7rem;
    }

    :global(body.layout-home .site-info-frame footer.site-footer .footer-copy strong) {
      font-size: 0.78rem;
    }

    :global(body.layout-home .site-info-frame footer.site-footer .footer-copy p) {
      display: none;
    }

    :global(body.layout-home .site-info-frame footer.site-footer .footer-links),
    :global(body.layout-home .site-info-frame footer.site-footer .footer-resources),
    :global(body.layout-home .site-info-frame footer.site-footer .footer-socials) {
      gap: 0.28rem;
    }

    :global(body.layout-home .site-info-frame footer.site-footer .footer-links a),
    :global(body.layout-home .site-info-frame footer.site-footer .footer-resources a),
    :global(body.layout-home .site-info-frame footer.site-footer .footer-socials a) {
      min-height: 1.45rem;
      font-size: 0.68rem;
    }

    :global(body.layout-home .site-info-frame footer.site-footer .footer-links a),
    :global(body.layout-home .site-info-frame footer.site-footer .footer-resources a) {
      padding: 0.16rem 0.48rem;
    }

    :global(body.layout-home .site-info-frame footer.site-footer .footer-socials a) {
      width: 1.45rem;
    }

    :global(body.layout-home .site-info-frame footer.site-footer .footer-actions) {
      gap: 0.28rem;
    }

    :global(body.layout-home .site-info-frame footer.site-footer .footer-bottom) {
      margin-top: 0.42rem;
      padding-top: 0.36rem;
      font-size: 0.66rem;
    }
  }

  @media (min-width: 1025px) and (max-height: 760px) {
    .site-info-frame {
      gap: 0.35rem;
      padding-block: 0.35rem;
    }

    :global(body.layout-home) .site-info-frame .site-info-section.home-section-shell {
      padding-block: 0.2rem;
    }

    .site-info-frame .site-content {
      gap: 0.45rem;
    }

    .site-info-frame .site-overview-card {
      padding: 0.55rem;
    }

    .site-info-frame .site-overview-icon {
      width: 2.1rem;
      height: 2.1rem;
    }

    .site-info-frame .site-overview-body p {
      -webkit-line-clamp: 1;
    }

    .site-info-frame .site-facts {
      display: none;
    }

    .site-info-frame .stats-grid {
      gap: 0.42rem;
    }

    .site-info-frame .stat-card > .relative,
    .site-info-frame .tech-card > .relative {
      padding: 0.46rem 0.54rem;
    }

    .site-info-frame .stat-icon {
      width: 1.85rem;
      height: 1.85rem;
    }

    .site-info-frame .stat-value {
      font-size: 0.96rem;
    }

    .site-info-frame .stat-label {
      font-size: 0.64rem;
    }

    .site-info-frame .tech-stack-section {
      padding: 0.58rem 0.66rem;
    }

    .site-info-frame .tech-description {
      display: none;
    }

    .site-info-frame .build-notes {
      display: none;
    }

    :global(body.layout-home .site-info-frame footer.site-footer .footer-inner) {
      padding: 0.36rem 0.65rem 0.32rem;
    }

    :global(body.layout-home .site-info-frame footer.site-footer .footer-main) {
      gap: 0.42rem;
    }

    :global(body.layout-home .site-info-frame footer.site-footer .footer-bottom) {
      margin-top: 0.28rem;
      padding-top: 0.24rem;
    }
  }

  /* Responsive optimization */
  @media (max-width: 1024px) {
    .stats-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 640px) {
    .stats-grid {
      grid-template-columns: 1fr;
    }

    .site-facts,
    .content-map-grid {
      grid-template-columns: 1fr;
    }
    
    .tech-grid {
      grid-template-columns: 1fr !important;
    }
  }
</style>
