---
/**
 * AboutSection Component
 * Personal info section for homepage
 * Displays bio, skills, tools, and interests
 */
import { getAboutConfig, getLocalizedUserContent, getUserProfile } from '../../data/site.config';
import type { AboutSectionItem } from '../../data/user.config';
import { useI18n } from '../../utils/i18n';

const { currentLang } = useI18n();
const user = getUserProfile();
const aboutConfig = getAboutConfig();
const userContent = getLocalizedUserContent(currentLang);
const isZh = currentLang === 'zh';
const labels = isZh
  ? {
      profile: '个人快照',
      now: '当前状态',
      education: '正在学习',
      timeline: '近期记录',
      focus: '关注方向',
      profileFocus: '最近聚焦',
      interests: '生活切片',
      channels: '可联系渠道',
      contact: '快速联系',
      status: '在线状态',
      available: '保持连接',
      tools: '常用工具',
      location: '常驻位置',
      more: '完整关于页',
      empty: '持续整理中',
    }
  : {
      profile: 'Profile',
      now: 'Now',
      education: 'Studying',
      timeline: 'Recent note',
      focus: 'Focus',
      profileFocus: 'Current focus',
      interests: 'Life fragments',
      channels: 'Channels',
      contact: 'Quick contact',
      status: 'Status',
      available: 'Available',
      tools: 'Tools',
      location: 'Location',
      more: 'Full About',
      empty: 'Being refined',
    };

const primaryEducation = aboutConfig.education[0];
const latestTimeline = aboutConfig.timeline[0];
const interestSection = aboutConfig.sections.find((section) => section.id === 'hobbies') ?? aboutConfig.sections[2];
const homeToolSections = aboutConfig.sections.slice(0, 2).map((section) => ({
  ...section,
  items: section.items.slice(0, 4),
}));
const focusChips = aboutConfig.sections
  .flatMap((section) => section.items.map((item: AboutSectionItem) => ({
    icon: item.icon ?? section.icon ?? 'i-carbon:dot-mark',
    name: item.name,
  })))
  .slice(0, 6);
const channelCount = aboutConfig.socialNetworks.length || user.socials.length;
const profileHighlights = [
  {
    icon: 'i-carbon:activity',
    label: labels.status,
    value: labels.available,
    tone: 'success',
  },
  {
    icon: focusChips[0]?.icon ?? 'i-carbon:center-circle',
    label: labels.profileFocus,
    value: focusChips[0]?.name ?? labels.empty,
    tone: 'secondary',
  },
  {
    icon: 'i-carbon:connect',
    label: labels.channels,
    value: `${channelCount}`,
    tone: 'primary',
  },
];
const contactLinks = user.socials.slice(0, 3);
const profileNotes = [
  {
    icon: 'i-carbon:time',
    label: labels.timeline,
    value: latestTimeline?.event ?? labels.empty,
  },
  {
    icon: interestSection?.icon ?? 'i-carbon:favorite',
    label: labels.interests,
    value: interestSection?.items.slice(0, 2).map((item) => item.name).join(' / ') ?? labels.empty,
  },
];
const snapshotCards = [
  {
    icon: 'i-carbon:location',
    label: labels.location,
    value: user.location,
  },
  {
    icon: 'i-carbon:education',
    label: labels.education,
    value: primaryEducation?.school ?? labels.empty,
  },
  {
    icon: 'i-carbon:connect',
    label: labels.channels,
    value: `${channelCount}`,
  },
];
---

<div class="home-section-frame about-home-frame h-full box-border px-4 py-6">
  <div class="about-section home-section-shell home-section-accent-secondary max-w-6xl mx-auto w-full">
    <!-- Section Header -->
    <div class="section-header home-section-heading mb-6 relative">
      <div class="absolute inset-0 -z-10 opacity-30">
        <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-64 h-64 bg-gradient-to-br from-[var(--md-sys-color-secondary)]/10 to-[var(--md-sys-color-primary)]/10 rounded-full blur-3xl"></div>
      </div>

      <span class="section-logo icon-wrapper inline-flex items-center justify-center bg-gradient-to-br from-[var(--md-sys-color-secondary-container)] to-[var(--md-sys-color-primary-container)] rounded-xl" aria-hidden="true">
        <i class="i-carbon:user-avatar text-xl text-[var(--md-sys-color-secondary)]"></i>
      </span>
      <h2 class="section-title inline-flex items-center justify-start gap-2 text-2xl md:text-3xl font-700 text-[var(--md-sys-color-on-surface)] mb-2">
        <span class="section-title-text bg-gradient-to-r from-[var(--md-sys-color-secondary)] to-[var(--md-sys-color-primary)] bg-clip-text text-transparent" data-i18n="about.title">
          关于我
        </span>
      </h2>
      <p class="section-subtitle text-xs md:text-sm text-[var(--md-sys-color-on-surface-variant)] font-500">
        <span class="section-subtitle-text" data-user-content="description">{userContent.description}</span>
      </p>

      <div class="section-divider flex items-center justify-start gap-2 mt-3">
        <div class="w-12 h-0.5 bg-gradient-to-r from-transparent to-[var(--md-sys-color-secondary)]/50"></div>
        <div class="w-2 h-2 rounded-full bg-[var(--md-sys-color-secondary)]"></div>
        <div class="w-12 h-0.5 bg-gradient-to-l from-transparent to-[var(--md-sys-color-secondary)]/50"></div>
      </div>
    </div>

    <!-- Main Content -->
    <div class="about-content home-section-content">
      <div class="about-profile-column">
        <!-- Bio Card -->
        <div class="card intro-card group relative animate-fade-in">
          <div class="absolute inset-0 bg-gradient-to-br from-[var(--md-sys-color-secondary)]/8 to-[var(--md-sys-color-primary)]/8 opacity-0 group-hover:opacity-100 transition-opacity duration-500 rounded-2xl pointer-events-none"></div>

          <div class="absolute -top-8 -right-8 w-32 h-32 bg-gradient-to-br from-[var(--md-sys-color-secondary)]/15 to-[var(--md-sys-color-primary)]/15 rounded-full blur-3xl group-hover:scale-150 transition-transform duration-700 pointer-events-none"></div>

          <div class="relative z-10 grid grid-cols-[auto_1fr] gap-4 md:gap-6">
            <div class="intro-left">
              <div class="avatar-wrapper relative">
                <div class="absolute inset-0 rounded-full bg-gradient-to-br from-[var(--md-sys-color-secondary)] to-[var(--md-sys-color-primary)] opacity-20 animate-pulse"></div>
                <div class="absolute inset-[-4px] rounded-full bg-gradient-to-br from-[var(--md-sys-color-secondary)] to-[var(--md-sys-color-primary)] opacity-0 group-hover:opacity-100 blur-sm transition-opacity duration-500"></div>

                <img
                  src={user.avatar}
                  alt={user.name}
                  class="avatar relative z-10"
                />

                <!-- 在线状态指示器 -->
                <div class="absolute bottom-1 right-1 z-20 w-4 h-4 rounded-full border-2 border-[var(--md-sys-color-surface)]" style="background: var(--reay-color-success); box-shadow: 0 0 14px rgba(var(--reay-color-success-rgb), 0.45);">
                  <div class="absolute inset-0 rounded-full animate-ping" style="background: var(--reay-color-success);"></div>
                </div>
              </div>
            </div>

            <div class="intro-right flex flex-col gap-2">
              <span class="profile-eyebrow">{labels.profile}</span>
              <h3 class="name text-xl md:text-2xl font-700 bg-gradient-to-r from-[var(--md-sys-color-secondary)] to-[var(--md-sys-color-primary)] bg-clip-text text-transparent mb-1">
                {user.name}
              </h3>

              <p class="tagline">{userContent.tagline}</p>

              <p class="location flex items-center gap-1.5 text-xs md:text-sm text-[var(--md-sys-color-on-surface-variant)]">
                <i class="i-carbon:location text-base text-[var(--md-sys-color-secondary)]"></i>
                {user.location}
              </p>

              <p class="bio text-xs md:text-sm leading-relaxed text-[var(--md-sys-color-on-surface-variant)]/90 mt-1" data-user-content="bio">
                {userContent.bio}
              </p>

              <div class="profile-meta-grid" aria-label={labels.profile}>
                {profileHighlights.map((item) => (
                  <div class={`profile-meta-card tone-${item.tone}`}>
                    <span class="profile-meta-icon" aria-hidden="true">
                      <i class={item.icon}></i>
                    </span>
                    <span class="profile-meta-label">{item.label}</span>
                    <strong>{item.value}</strong>
                  </div>
                ))}
              </div>

              <!-- 社交链接 - 美化版 -->
              <div class="social-links" aria-label={labels.contact}>
                {contactLinks.map((social, index) => (
                  <a
                    href={social.url}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="social-link group/link"
                    aria-label={social.label}
                    title={social.label}
                    style={`animation-delay: ${index * 100 + 300}ms;`}
                  >
                    <span class="social-icon-wrapper">
                      <i class={social.icon}></i>
                    </span>
                    <span class="social-label">{social.label}</span>
                    <i class="i-carbon:arrow-up-right text-xs opacity-0 group-hover/link:opacity-100 transition-all duration-300 group-hover/link:translate-x-0.5 group-hover/link:-translate-y-0.5"></i>
                  </a>
                ))}
              </div>

              <div class="profile-note-strip" aria-label={labels.timeline}>
                {profileNotes.map((item) => (
                  <div class="profile-note-card">
                    <span class="profile-note-icon" aria-hidden="true">
                      <i class={item.icon}></i>
                    </span>
                    <span class="profile-note-label">{item.label}</span>
                    <strong>{item.value}</strong>
                  </div>
                ))}
              </div>
            </div>
          </div>
        </div>

        <div class="profile-snapshot-grid">
          {snapshotCards.map((card) => (
            <div class="snapshot-card">
              <span class="snapshot-icon" aria-hidden="true">
                <i class={card.icon}></i>
              </span>
              <span class="snapshot-label">{card.label}</span>
              <strong>{card.value}</strong>
            </div>
          ))}
        </div>
      </div>

      <div class="about-detail-column">
        <div class="about-current-card animate-fade-in" style="animation-delay: 250ms;">
          <div class="about-card-heading">
            <div>
              <span>{labels.now}</span>
              <h3>{labels.focus}</h3>
            </div>
            <a href="/about" class="about-more-link">
              <span>{labels.more}</span>
              <i class="i-carbon:arrow-up-right"></i>
            </a>
          </div>

          <div class="current-items">
            {primaryEducation && (
              <div class="current-item">
                <span class="current-marker">
                  <i class="i-carbon:education"></i>
                </span>
                <div>
                  <span class="current-label">{labels.education}</span>
                  <strong>{primaryEducation.school}</strong>
                  <p>{[primaryEducation.major, primaryEducation.degree].filter(Boolean).join(' · ') || primaryEducation.description}</p>
                </div>
              </div>
            )}

            {latestTimeline && (
              <div class="current-item">
                <span class="current-marker">
                  <i class="i-carbon:time"></i>
                </span>
                <div>
                  <span class="current-label">{latestTimeline.year}</span>
                  <strong>{latestTimeline.event}</strong>
                  {latestTimeline.description && <p>{latestTimeline.description}</p>}
                </div>
              </div>
            )}
          </div>

          <div class="focus-chip-grid">
            {focusChips.map((chip) => (
              <span class="focus-chip">
                <i class={chip.icon}></i>
                {chip.name}
              </span>
            ))}
          </div>
        </div>

        <div class="about-tools-stack">
          <!-- 技能/工具网格 - 美化版 -->
          {homeToolSections.map((section, sectionIndex) => (
            <div class="tools-section animate-fade-in" style={`animation-delay: ${sectionIndex * 150 + 400}ms;`}>
              <div class="tools-header flex items-center gap-2 mb-2">
                <div class="icon-wrapper inline-flex items-center justify-center w-8 h-8 bg-gradient-to-br from-[var(--md-sys-color-secondary-container)] to-[var(--md-sys-color-primary-container)] rounded-lg">
                  <i class={`${section.icon} text-base text-[var(--md-sys-color-secondary)]`}></i>
                </div>
                <h3 class="text-base md:text-lg font-700 text-[var(--md-sys-color-on-surface)]" data-i18n={`about.${section.id}.title`}>{section.title}</h3>
              </div>
              <p class="tools-description text-xs text-[var(--md-sys-color-on-surface-variant)] mb-3" data-i18n={`about.${section.id}.subtitle`}>{section.description}</p>

              <div class={`tools-grid cols-${section.columns} ${section.compact ? 'compact' : ''}`}>
                {section.items.map((item: AboutSectionItem, itemIndex: number) => (
                  <a
                    href={item.url || '#'}
                    target={item.url ? '_blank' : undefined}
                    rel={item.url ? 'noopener noreferrer' : undefined}
                    class={`card-minimal tool-card group/tool animate-scale-in`}
                    style={`animation-delay: ${itemIndex * 50 + sectionIndex * 150 + 500}ms;`}
                  >
                    <!-- 装饰性背景 -->
                    <div class="absolute inset-0 bg-gradient-to-br from-[var(--md-sys-color-secondary)]/8 to-[var(--md-sys-color-primary)]/8 opacity-0 group-hover/tool:opacity-100 transition-opacity duration-400 rounded-xl pointer-events-none"></div>

                    {item.icon && (
                      <div class="tool-icon-wrapper relative z-10">
                        <i class={`${item.icon} text-2xl text-[var(--md-sys-color-secondary)] group-hover/tool:scale-110 transition-transform duration-300`}></i>
                      </div>
                    )}

                    <div class="tool-info relative z-10">
                      <h4 class="text-sm font-600 text-[var(--md-sys-color-on-surface)] group-hover/tool:text-[var(--md-sys-color-secondary)] transition-colors leading-snug">
                        {item.name}
                      </h4>
                      {item.description && (
                        <p class="text-xs text-[var(--md-sys-color-on-surface-variant)]/80 leading-relaxed mt-0.5 line-clamp-2">
                          {item.description}
                        </p>
                      )}
                    </div>

                    <!-- 外链指示器 -->
                    {item.url && (
                      <i class="i-carbon:arrow-up-right absolute top-2 right-2 text-sm text-[var(--md-sys-color-on-surface-variant)] opacity-0 group-hover/tool:opacity-100 transition-all duration-300 transform translate-x-1 -translate-y-1 group-hover/tool:translate-x-0 group-hover/tool:translate-y-0"></i>
                    )}
                  </a>
                ))}
              </div>
            </div>
          ))}
        </div>

        {interestSection && (
          <div class="interest-card animate-fade-in" style="animation-delay: 620ms;">
            <div class="interest-heading">
              <span>{labels.interests}</span>
              <strong data-i18n={`about.${interestSection.id}.title`}>{interestSection.title}</strong>
            </div>
            <div class="interest-grid">
              {interestSection.items.slice(0, 4).map((item) => (
                <span class="interest-chip">
                  {item.icon && <i class={item.icon}></i>}
                  {item.name}
                </span>
              ))}
            </div>
          </div>
        )}
      </div>
    </div>
  </div>
</div>

<style>
  /* Layout */
  .about-home-frame {
    display: flex;
    align-items: center;
    justify-content: center;
    overflow-y: auto;
  }

  .about-section {
    max-width: 1200px;
    width: 100%;
  }

  .about-content,
  .about-profile-column,
  .about-detail-column,
  .about-tools-stack {
    display: grid;
    gap: 1rem;
    min-width: 0;
  }

  .profile-eyebrow,
  .current-label,
  .snapshot-label,
  .interest-heading span,
  .about-card-heading span {
    color: var(--md-sys-color-secondary);
    font-size: 0.66rem;
    font-weight: 800;
    letter-spacing: 0;
    text-transform: uppercase;
  }

  /* Avatar wrapper with hover animation */
  .avatar-wrapper {
    position: relative;
    width: 80px;
    height: 80px;
  }

  @media (min-width: 768px) {
    .avatar-wrapper {
      width: 100px;
      height: 100px;
    }
  }

  .avatar {
    width: 100%;
    height: 100%;
    border-radius: 50%;
    object-fit: cover;
    box-shadow: 0 8px 24px rgba(var(--md-sys-color-shadow-rgb), 0.15);
    border: 3px solid var(--md-sys-color-surface);
    transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .intro-card:hover .avatar {
    transform: scale(1.05);
  }

  /* Introduction card with glass-card base styles */
  .intro-card {
    overflow: hidden;
  }

  @media (min-width: 768px) {
    .intro-card {
      padding: 2rem;
    }
  }

  .intro-card:hover {
    border-color: var(--md-sys-color-secondary);
  }

  .intro-left {
    display: flex;
    align-items: start;
  }

  .intro-right {
    min-width: 0;
  }

  .name {
    margin: 0;
    line-height: 1.2;
  }

  .location {
    margin: 0;
    font-weight: 500;
  }

  .bio {
    margin: 0;
  }

  .tagline {
    margin: -0.1rem 0 0;
    color: var(--md-sys-color-on-surface);
    font-size: 0.76rem;
    font-weight: 750;
    line-height: 1.4;
  }

  .profile-meta-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0.45rem;
    margin-top: 0.4rem;
  }

  .profile-meta-card {
    position: relative;
    display: grid;
    grid-template-areas:
      "icon label"
      "icon value";
    grid-template-columns: auto minmax(0, 1fr);
    column-gap: 0.45rem;
    row-gap: 0.04rem;
    align-items: center;
    min-width: 0;
    overflow: hidden;
    padding: 0.52rem 0.58rem;
    border: 1px solid rgba(var(--md-sys-color-outline-variant-rgb), 0.36);
    border-radius: var(--reay-radius-md, 1rem);
    background:
      linear-gradient(135deg, rgba(var(--md-sys-color-surface-container-high-rgb), 0.58), rgba(var(--md-sys-color-surface-container-low-rgb), 0.38));
    box-shadow: inset 0 1px 0 rgba(var(--md-sys-color-on-surface-rgb), 0.05);
  }

  .profile-meta-card::before {
    content: '';
    position: absolute;
    inset: 0;
    opacity: 0.52;
    background: radial-gradient(circle at 12% 16%, rgba(var(--md-sys-color-secondary-rgb), 0.18), transparent 44%);
    pointer-events: none;
  }

  .profile-meta-icon {
    position: relative;
    grid-area: icon;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.55rem;
    height: 1.55rem;
    border-radius: 0.68rem;
    color: var(--md-sys-color-secondary);
    background: rgba(var(--md-sys-color-secondary-container-rgb), 0.5);
  }

  .profile-meta-icon i {
    font-size: 0.92rem;
  }

  .profile-meta-label {
    position: relative;
    grid-area: label;
    overflow: hidden;
    color: var(--md-sys-color-on-surface-variant);
    font-size: 0.56rem;
    font-weight: 760;
    letter-spacing: 0;
    line-height: 1.1;
    text-overflow: ellipsis;
    text-transform: uppercase;
    white-space: nowrap;
  }

  .profile-meta-card strong {
    position: relative;
    grid-area: value;
    overflow: hidden;
    color: var(--md-sys-color-on-surface);
    font-size: 0.72rem;
    font-weight: 820;
    line-height: 1.2;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .profile-meta-card.tone-success .profile-meta-icon {
    color: var(--reay-color-success);
    background: rgba(var(--reay-color-success-rgb), 0.14);
    box-shadow: 0 0 16px rgba(var(--reay-color-success-rgb), 0.16);
  }

  .profile-meta-card.tone-primary .profile-meta-icon {
    color: var(--md-sys-color-primary);
    background: rgba(var(--md-sys-color-primary-container-rgb), 0.46);
  }

  .profile-note-strip {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.48rem;
    margin-top: 0.35rem;
  }

  .profile-note-card {
    position: relative;
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    grid-template-areas:
      "icon label"
      "icon value";
    align-items: center;
    column-gap: 0.48rem;
    min-width: 0;
    padding: 0.58rem 0.62rem;
    overflow: hidden;
    border: 1px solid rgba(var(--md-sys-color-outline-variant-rgb), 0.34);
    border-radius: var(--reay-radius-md, 1rem);
    background:
      radial-gradient(circle at 90% 8%, rgba(var(--md-sys-color-primary-rgb), 0.12), transparent 40%),
      rgba(var(--md-sys-color-surface-container-high-rgb), 0.36);
  }

  .profile-note-icon {
    grid-area: icon;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.7rem;
    height: 1.7rem;
    border-radius: 0.7rem;
    color: var(--md-sys-color-primary);
    background: rgba(var(--md-sys-color-primary-container-rgb), 0.42);
  }

  .profile-note-icon i {
    font-size: 0.92rem;
  }

  .profile-note-label {
    grid-area: label;
    overflow: hidden;
    color: var(--md-sys-color-on-surface-variant);
    font-size: 0.58rem;
    font-weight: 760;
    line-height: 1.1;
    text-overflow: ellipsis;
    text-transform: uppercase;
    white-space: nowrap;
  }

  .profile-note-card strong {
    grid-area: value;
    overflow: hidden;
    color: var(--md-sys-color-on-surface);
    font-size: 0.72rem;
    font-weight: 800;
    line-height: 1.2;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .profile-snapshot-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0.55rem;
  }

  .snapshot-card {
    position: relative;
    display: grid;
    gap: 0.22rem;
    min-width: 0;
    padding: 0.68rem;
    overflow: hidden;
    border: 1px solid rgba(var(--md-sys-color-outline-variant-rgb), 0.38);
    border-radius: var(--reay-radius-md, 1rem);
    background:
      linear-gradient(135deg, rgba(var(--md-sys-color-secondary-container-rgb), 0.34), rgba(var(--md-sys-color-surface-container-rgb), 0.48));
    box-shadow: var(--reay-surface-shadow);
  }

  .snapshot-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.8rem;
    height: 1.8rem;
    border-radius: 0.7rem;
    color: var(--md-sys-color-secondary);
    background: rgba(var(--md-sys-color-secondary-container-rgb), 0.52);
  }

  .snapshot-card strong {
    overflow: hidden;
    color: var(--md-sys-color-on-surface);
    font-size: 0.78rem;
    line-height: 1.25;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .about-current-card,
  .interest-card {
    position: relative;
    overflow: hidden;
    border: 1px solid rgba(var(--md-sys-color-outline-variant-rgb), 0.42);
    border-radius: var(--reay-radius-lg, 1.35rem);
    background:
      radial-gradient(circle at 92% 0%, rgba(var(--md-sys-color-secondary-rgb), 0.14), transparent 36%),
      linear-gradient(135deg, rgba(var(--md-sys-color-surface-container-low-rgb), 0.76), rgba(var(--md-sys-color-surface-container-rgb), 0.58));
    box-shadow: var(--reay-surface-shadow);
    backdrop-filter: blur(var(--reay-glass-blur, 14px)) saturate(150%);
    -webkit-backdrop-filter: blur(var(--reay-glass-blur, 14px)) saturate(150%);
  }

  .about-current-card {
    display: grid;
    gap: 0.75rem;
    padding: 1rem;
  }

  .about-card-heading {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.8rem;
  }

  .about-card-heading h3 {
    margin: 0.16rem 0 0;
    color: var(--md-sys-color-on-surface);
    font-size: 1.04rem;
    font-weight: 820;
    line-height: 1.15;
  }

  .about-more-link {
    display: inline-flex;
    align-items: center;
    gap: 0.28rem;
    flex: 0 0 auto;
    min-height: 1.75rem;
    padding: 0.22rem 0.58rem;
    border: 1px solid rgba(var(--md-sys-color-secondary-rgb), 0.28);
    border-radius: 999px;
    color: var(--md-sys-color-secondary);
    background: rgba(var(--md-sys-color-secondary-container-rgb), 0.26);
    font-size: 0.68rem;
    font-weight: 760;
    text-decoration: none;
    transition: transform 0.22s var(--reay-ease-out), border-color 0.22s ease, background 0.22s ease;
  }

  .about-more-link:hover {
    border-color: rgba(var(--md-sys-color-secondary-rgb), 0.5);
    background: rgba(var(--md-sys-color-secondary-container-rgb), 0.46);
    transform: translateY(-1px);
  }

  .current-items {
    display: grid;
    gap: 0.55rem;
  }

  .current-item {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    gap: 0.65rem;
    min-width: 0;
    padding: 0.72rem;
    border: 1px solid rgba(var(--md-sys-color-outline-variant-rgb), 0.32);
    border-radius: var(--reay-radius-md, 1rem);
    background: rgba(var(--md-sys-color-surface-container-high-rgb), 0.34);
  }

  .current-marker {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2rem;
    height: 2rem;
    border-radius: 0.78rem;
    color: var(--md-sys-color-secondary);
    background: rgba(var(--md-sys-color-secondary-container-rgb), 0.54);
  }

  .current-item strong {
    display: block;
    margin-top: 0.1rem;
    overflow: hidden;
    color: var(--md-sys-color-on-surface);
    font-size: 0.84rem;
    line-height: 1.25;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .current-item p {
    display: -webkit-box;
    margin: 0.24rem 0 0;
    overflow: hidden;
    color: var(--md-sys-color-on-surface-variant);
    font-size: 0.7rem;
    line-height: 1.42;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 2;
  }

  .focus-chip-grid,
  .interest-grid {
    display: flex;
    flex-wrap: wrap;
    gap: 0.42rem;
  }

  .focus-chip,
  .interest-chip {
    display: inline-flex;
    align-items: center;
    gap: 0.34rem;
    min-width: 0;
    min-height: 1.65rem;
    padding: 0.2rem 0.54rem;
    border: 1px solid rgba(var(--md-sys-color-outline-variant-rgb), 0.34);
    border-radius: 999px;
    color: var(--md-sys-color-on-surface-variant);
    background: rgba(var(--md-sys-color-surface-container-high-rgb), 0.36);
    font-size: 0.68rem;
    font-weight: 700;
  }

  .focus-chip i,
  .interest-chip i {
    color: var(--md-sys-color-secondary);
    font-size: 0.9rem;
  }

  .interest-card {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    align-items: center;
    gap: 0.8rem;
    padding: 0.78rem 0.9rem;
  }

  .interest-heading {
    display: grid;
    gap: 0.12rem;
  }

  .interest-heading strong {
    color: var(--md-sys-color-on-surface);
    font-size: 0.88rem;
    line-height: 1.2;
  }

  /* Social links with pill-style design */
  .social-links {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0.5rem;
    width: 100%;
  }

  .social-link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    min-width: 0;
    padding: 0.5rem 0.875rem;
    background: var(--md-sys-color-surface-container-high);
    border: 1px solid var(--md-sys-color-outline-variant);
    border-radius: 999px;
    text-decoration: none;
    color: var(--md-sys-color-on-surface-variant);
    font-size: 0.75rem;
    font-weight: 600;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
    opacity: 0;
    animation: fade-in 0.6s cubic-bezier(0.4, 0, 0.2, 1) forwards;
  }

  @media (min-width: 768px) {
    .social-link {
      font-size: 0.8125rem;
    }
  }

  .social-link::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg,
      var(--md-sys-color-secondary-container),
      var(--md-sys-color-primary-container)
    );
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .social-link:hover {
    border-color: var(--md-sys-color-secondary);
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(var(--md-sys-color-shadow-rgb), 0.1);
  }

  .social-link:hover::before {
    opacity: 1;
  }

  .social-icon-wrapper {
    position: relative;
    z-index: 1;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .social-icon-wrapper i {
    font-size: 1rem;
    transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .social-link:hover .social-icon-wrapper i {
    transform: scale(1.1);
  }

  .social-label {
    position: relative;
    z-index: 1;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  /* Tools section layout */
  .tools-section {
    margin-bottom: 1rem;
  }

  .tools-header {
    margin-bottom: 0.75rem;
  }

  .tools-description {
    margin: 0;
    line-height: 1.6;
  }

  .tools-grid {
    display: grid;
    gap: 0.75rem;
  }

  .tools-grid.cols-2 {
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  }

  .tools-grid.cols-3 {
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  }

  .tools-grid.cols-4 {
    grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
  }

  .tools-grid.compact {
    gap: 0.5rem;
  }

  @media (min-width: 768px) {
    .tools-grid {
      gap: 1rem;
    }

    .tools-grid.compact {
      gap: 0.75rem;
    }
  }

  .tool-card {
    position: relative;
    display: flex;
    align-items: center;
    gap: 0.75rem;
    text-decoration: none;
    color: inherit;
    overflow: hidden;
    opacity: 0;
    transform: scale(0.9);
  }

  @media (min-width: 768px) {
    .tool-card {
      padding: 1.125rem;
    }
  }

  .tool-card:hover {
    border-color: var(--md-sys-color-secondary);
    transform: translateY(-4px) scale(1);
  }

  .tool-icon-wrapper {
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.5rem;
    height: 2.5rem;
  }

  .tool-info {
    flex: 1;
    min-width: 0;
  }

  .tool-info h4 {
    margin: 0;
    line-height: 1.3;
  }

  .tool-info p {
    margin: 0;
    line-clamp: 2;
    line-height: 1.4;
  }

  @media (min-width: 1025px) {
    .about-home-frame {
      overflow: hidden;
      padding-block: clamp(0.7rem, 1.4vh, 1rem);
    }

    :global(body.layout-home) .about-section.home-section-shell {
      min-height: 0;
      height: min(100%, calc(100dvh - 7rem));
      align-items: center;
    }

    .about-content {
      grid-template-columns: minmax(18rem, 0.82fr) minmax(0, 1.18fr);
      align-items: stretch;
      gap: clamp(0.75rem, 1.4vw, 1.05rem);
    }

    .about-profile-column {
      grid-template-rows: minmax(0, 1fr) auto;
      align-self: stretch;
      gap: 0.7rem;
      min-height: 0;
    }

    .about-detail-column {
      grid-template-rows: auto minmax(0, 1fr) auto;
      gap: 0.7rem;
      min-height: 0;
    }

    .intro-card {
      height: 100%;
      min-height: 0;
      padding: clamp(1rem, 1.55vw, 1.45rem);
      border-radius: var(--reay-radius-lg, 1.35rem);
    }

    .intro-card > .relative {
      height: 100%;
      grid-template-columns: 1fr;
      grid-template-rows: auto minmax(0, 1fr);
      align-content: stretch;
      justify-items: start;
      gap: clamp(0.72rem, 1.3vw, 1rem);
    }

    .intro-left {
      justify-content: flex-start;
    }

    .avatar-wrapper {
      width: clamp(4.5rem, 7vw, 5.75rem);
      height: clamp(4.5rem, 7vw, 5.75rem);
    }

    .intro-right {
      gap: 0.36rem;
      height: 100%;
    }

    .name {
      font-size: clamp(1.25rem, 2vw, 1.65rem);
    }

    .bio {
      display: -webkit-box;
      overflow: hidden;
      line-height: 1.65;
      -webkit-box-orient: vertical;
      -webkit-line-clamp: 2;
    }

    .profile-meta-grid {
      gap: 0.36rem;
      margin-top: 0.28rem;
    }

    .profile-meta-card {
      padding: 0.42rem 0.46rem;
      border-radius: 0.82rem;
    }

    .profile-meta-icon {
      width: 1.32rem;
      height: 1.32rem;
      border-radius: 0.56rem;
    }

    .profile-meta-icon i {
      font-size: 0.78rem;
    }

    .profile-meta-card strong {
      font-size: 0.64rem;
    }

    .social-links {
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 0.36rem;
      margin-top: 0.35rem;
    }

    .social-link {
      gap: 0.36rem;
      padding: 0.42rem 0.48rem;
      font-size: 0.68rem;
    }

    .profile-note-strip {
      gap: 0.36rem;
      margin-top: auto;
    }

    .profile-note-card {
      padding: 0.42rem 0.46rem;
      border-radius: 0.82rem;
    }

    .profile-note-icon {
      width: 1.32rem;
      height: 1.32rem;
      border-radius: 0.56rem;
    }

    .profile-note-icon i {
      font-size: 0.78rem;
    }

    .profile-note-card strong {
      font-size: 0.64rem;
    }

    .profile-snapshot-grid {
      gap: 0.45rem;
    }

    .snapshot-card {
      padding: 0.52rem;
    }

    .snapshot-icon {
      width: 1.55rem;
      height: 1.55rem;
      border-radius: 0.62rem;
    }

    .snapshot-card strong {
      font-size: 0.68rem;
    }

    .about-current-card {
      gap: 0.58rem;
      padding: 0.68rem 0.76rem;
      border-radius: var(--reay-radius-lg, 1.35rem);
    }

    .about-card-heading h3 {
      font-size: 0.94rem;
    }

    .current-items {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 0.46rem;
    }

    .current-item {
      gap: 0.48rem;
      padding: 0.54rem;
    }

    .current-marker {
      width: 1.7rem;
      height: 1.7rem;
      border-radius: 0.68rem;
    }

    .current-item strong {
      font-size: 0.72rem;
    }

    .current-item p {
      font-size: 0.64rem;
      -webkit-line-clamp: 1;
    }

    .focus-chip-grid {
      gap: 0.32rem;
    }

    .focus-chip,
    .interest-chip {
      min-height: 1.35rem;
      padding: 0.12rem 0.42rem;
      font-size: 0.62rem;
    }

    .about-tools-stack {
      grid-template-rows: repeat(2, minmax(0, 1fr));
      gap: 0.55rem;
      min-height: 0;
    }

    .tools-section {
      position: relative;
      min-height: 0;
      margin-bottom: 0;
      padding: clamp(0.75rem, 1.25vw, 0.95rem);
      overflow: hidden;
      border: 1px solid rgba(var(--md-sys-color-outline-variant-rgb), 0.42);
      border-radius: var(--reay-radius-lg, 1.35rem);
      background:
        radial-gradient(circle at 92% 0%, rgba(var(--md-sys-color-secondary-rgb), 0.12), transparent 36%),
        linear-gradient(135deg, rgba(var(--md-sys-color-surface-container-low-rgb), 0.76), rgba(var(--md-sys-color-surface-container-rgb), 0.58));
      box-shadow: var(--reay-surface-shadow);
      backdrop-filter: blur(var(--reay-glass-blur, 14px)) saturate(150%);
      -webkit-backdrop-filter: blur(var(--reay-glass-blur, 14px)) saturate(150%);
    }

    .tools-header {
      margin-bottom: 0.38rem;
    }

    .tools-header .icon-wrapper {
      width: 2rem;
      height: 2rem;
      border-radius: 0.75rem;
    }

    .tools-header h3 {
      font-size: clamp(0.92rem, 1.35vw, 1.05rem);
    }

    .tools-description {
      display: -webkit-box;
      margin-bottom: 0.55rem;
      overflow: hidden;
      line-height: 1.45;
      -webkit-box-orient: vertical;
      -webkit-line-clamp: 1;
    }

    .about-section .tools-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr)) !important;
      gap: 0.5rem;
    }

    .tool-card {
      min-height: 0;
      gap: 0.55rem;
      padding: 0.56rem 0.64rem;
      border-radius: 0.9rem;
    }

    .tool-icon-wrapper {
      width: 1.75rem;
      height: 1.75rem;
    }

    .tool-icon-wrapper i {
      font-size: 1.15rem;
    }

    .tool-info h4 {
      font-size: 0.78rem;
    }

    .tool-info p {
      display: -webkit-box;
      overflow: hidden;
      font-size: 0.68rem;
      line-height: 1.35;
      -webkit-box-orient: vertical;
      -webkit-line-clamp: 1;
    }
  }

  @media (min-width: 1025px) and (max-height: 760px) {
    .about-home-frame {
      padding-block: 0.45rem;
    }

    :global(body.layout-home) .about-section.home-section-shell {
      height: min(100%, calc(100dvh - 6rem));
    }

    .intro-card {
      padding: 0.95rem;
    }

    .bio {
      -webkit-line-clamp: 2;
    }

    .profile-meta-grid {
      gap: 0.28rem;
    }

    .profile-meta-card {
      padding: 0.34rem 0.38rem;
    }

    .profile-note-strip {
      display: none;
    }

    .tools-section {
      padding: 0.62rem;
    }

    .tools-description {
      display: none;
    }

    .tool-card {
      padding: 0.48rem 0.56rem;
    }

    .profile-snapshot-grid,
    .focus-chip-grid,
    .interest-card {
      display: none;
    }

    .about-current-card {
      padding: 0.58rem 0.66rem;
    }

    .current-items {
      grid-template-columns: 1fr 1fr;
    }
  }

  /* Text truncation utility */
  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  /* Animations */
  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes scale-in {
    from {
      opacity: 0;
      transform: scale(0.9);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  .animate-fade-in {
    opacity: 0;
    animation: fade-in 0.6s cubic-bezier(0.4, 0, 0.2, 1) forwards;
  }

  .animate-scale-in {
    animation: scale-in 0.4s cubic-bezier(0.4, 0, 0.2, 1) forwards;
  }

  /* Icon wrapper pulsing glow animation */
  .section-header .icon-wrapper {
    animation: pulse-glow 2s ease-in-out infinite;
  }

  @keyframes pulse-glow {
    0%, 100% {
      box-shadow: 0 0 0 0 var(--md-sys-color-secondary);
      opacity: 1;
    }
    50% {
      box-shadow: 0 0 20px 5px var(--md-sys-color-secondary);
      opacity: 0.8;
    }
  }

  /* Responsive optimization */
  @media (max-width: 640px) {
    .tools-grid {
      grid-template-columns: 1fr !important;
    }

    .intro-card > .relative {
      grid-template-columns: 1fr;
    }

    .profile-meta-grid {
      grid-template-columns: 1fr;
    }

    .profile-note-strip {
      grid-template-columns: 1fr;
    }
  }
</style>

<script>
  import { initI18n } from '../../scripts/i18n-content-updater';
  import { initUserContentI18n } from '../../scripts/i18n-user-content';

  initI18n();
  initUserContentI18n();
</script>
