---
/**
 * ProjectDetailHeader.astro
 * 项目详情页头部组件
 */
import { formatNumber, getLanguageColor } from '../../utils/github';
import { useI18n } from '../../utils/i18n';

interface Props {
  project: {
    name: string;
    description: string | null;
    url: string;
    homepage: string | null;
    stars: number;
    forks: number;
    language: string | null;
    license: string | null;
    topics: string[];
    createdAt: string;
    updatedAt: string;
  };
}

const { project } = Astro.props;
const { t } = useI18n();

const createdDate = new Date(project.createdAt).toLocaleDateString('zh-CN');
const updatedDate = new Date(project.updatedAt).toLocaleDateString('zh-CN');
---

<header class="project-header">
  <!-- 项目标题区域 -->
  <div class="project-title-section">
    <div class="title-wrapper">
      <div class="icon-wrapper">
        <i class="i-carbon:folder"></i>
      </div>
      <div class="title-content">
        <h1 class="project-title">{project.name}</h1>
        {project.description && (
          <p class="project-subtitle">{project.description}</p>
        )}
      </div>
    </div>
  </div>

  <!-- 统计卡片 -->
  <div class="stats-cards">
    <div class="stat-card">
      <div class="stat-icon star">
        <i class="i-carbon:star-filled"></i>
      </div>
      <div class="stat-content">
        <div class="stat-value">{formatNumber(project.stars)}</div>
        <div class="stat-label" data-i18n="projects.stats.stars">{t('projects.stats.stars')}</div>
      </div>
    </div>
    <div class="stat-card">
      <div class="stat-icon fork">
        <i class="i-carbon:branch"></i>
      </div>
      <div class="stat-content">
        <div class="stat-value">{formatNumber(project.forks)}</div>
        <div class="stat-label" data-i18n="projects.stats.forks">{t('projects.stats.forks')}</div>
      </div>
    </div>
    {project.language && (
      <div class="stat-card">
        <div class="stat-icon language">
          <i class="i-carbon:code"></i>
        </div>
        <div class="stat-content">
          <div class="stat-value" style={`color: ${getLanguageColor(project.language)}`}>
            {project.language}
          </div>
          <div class="stat-label" data-i18n="projects.detail.language">{t('projects.detail.language')}</div>
        </div>
      </div>
    )}
    {project.license && (
      <div class="stat-card">
        <div class="stat-icon license">
          <i class="i-carbon:license"></i>
        </div>
        <div class="stat-content">
          <div class="stat-value">{project.license}</div>
          <div class="stat-label" data-i18n="projects.detail.license">{t('projects.detail.license')}</div>
        </div>
      </div>
    )}
  </div>

  <!-- 标签（移到中间位置） -->
  {project.topics.length > 0 && (
    <div class="project-topics">
      <div class="topics-label">
        <i class="i-carbon:tag"></i>
        <span data-i18n="projects.detail.tags">{t('projects.detail.tags')}</span>
      </div>
      <div class="topics-list">
        {project.topics.map(topic => (
          <span class="topic-tag">
            <span class="tag-hash">#</span>
            {topic}
          </span>
        ))}
      </div>
    </div>
  )}

  <!-- 操作区域 -->
  <div class="actions-section">
    <div class="action-buttons">
      <a
        href={project.url}
        target="_blank"
        rel="noopener noreferrer"
        class="action-button primary"
      >
        <i class="i-carbon:logo-github"></i>
        <span data-i18n="projects.detail.github">{t('projects.detail.github')}</span>
        <i class="i-carbon:arrow-right arrow"></i>
      </a>
      {project.homepage && (
        <a
          href={project.homepage}
          target="_blank"
          rel="noopener noreferrer"
          class="action-button secondary"
        >
          <i class="i-carbon:launch"></i>
          <span data-i18n="projects.detail.preview">{t('projects.detail.preview')}</span>
          <i class="i-carbon:arrow-right arrow"></i>
        </a>
      )}
    </div>

    <div class="project-dates">
      <div class="date-item">
        <i class="i-carbon:calendar"></i>
        <span><span data-i18n="projects.detail.created">{t('projects.detail.created')}</span>：{createdDate}</span>
      </div>
      <div class="date-separator">•</div>
      <div class="date-item">
        <i class="i-carbon:time"></i>
        <span><span data-i18n="projects.detail.updated">{t('projects.detail.updated')}</span>：{updatedDate}</span>
      </div>
    </div>
  </div>
</header>

<style>
  .project-header {
    background: var(--reay-surface-bg);
    border-radius: 1.1rem;
    padding: 1.25rem;
    margin-bottom: 1.5rem;
    border: 1px solid var(--reay-surface-border);
    box-shadow: var(--reay-surface-shadow);
    overflow: hidden;
    position: relative;
  }

  /* 标题区域 */
  .project-title-section {
    margin-bottom: 1rem;
  }

  .title-wrapper {
    display: flex;
    align-items: flex-start;
    gap: 0.875rem;
  }

  .icon-wrapper {
    flex-shrink: 0;
    width: 40px;
    height: 40px;
    border-radius: 8px;
    background: var(--md-sys-color-primary);
    color: var(--md-sys-color-on-primary);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.25rem;
  }

  .title-content {
    flex: 1;
    min-width: 0;
  }

  .project-title {
    font-size: 1.5rem;
    font-weight: 700;
    margin: 0 0 0.25rem 0;
    color: var(--md-sys-color-on-primary-container);
    line-height: 1.2;
    word-break: break-word;
    letter-spacing: 0;
  }

  .project-subtitle {
    font-size: 0.875rem;
    line-height: 1.5;
    color: var(--md-sys-color-on-primary-container);
    opacity: 0.85;
    margin: 0;
    word-break: break-word;
  }

  /* 统计卡片 */
  .stats-cards {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
    gap: 0.625rem;
    margin-bottom: 1rem;
  }

  .stat-card {
    background: var(--reay-surface-bg);
    border-radius: 0.9rem;
    padding: 0.75rem;
    display: flex;
    align-items: center;
    gap: 0.625rem;
    border: 1px solid var(--reay-surface-border);
    box-shadow: var(--reay-surface-shadow);
    backdrop-filter: blur(var(--reay-glass-blur)) saturate(150%);
    -webkit-backdrop-filter: blur(var(--reay-glass-blur)) saturate(150%);
    transition:
      transform 0.32s var(--reay-ease-out),
      background 0.32s ease,
      border-color 0.32s ease,
      box-shadow 0.32s ease;
    cursor: default;
    position: relative;
    overflow: hidden;
  }

  .stat-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(
      135deg,
      rgba(var(--md-sys-color-primary-rgb), 0.05),
      transparent
    );
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .stat-card:hover {
    transform: translateY(-2px);
    background: var(--reay-surface-bg-hover);
    border-color: var(--reay-surface-border-hover);
    box-shadow: var(--reay-surface-shadow-hover);
  }

  .stat-card:hover::before {
    opacity: 1;
  }

  .stat-icon {
    flex-shrink: 0;
    width: 32px;
    height: 32px;
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.125rem;
    transition: transform 0.3s ease;
  }

  .stat-card:hover .stat-icon {
    transform: scale(1.05) rotate(3deg);
  }

  .stat-icon.star {
    background: linear-gradient(135deg, var(--md-sys-color-tertiary-fixed), var(--md-sys-color-tertiary-fixed-dim));
    color: var(--md-sys-color-on-tertiary-fixed-variant);
    box-shadow: 0 4px 12px rgba(var(--md-sys-color-tertiary-rgb), 0.3);
  }

  .stat-icon.fork {
    background: linear-gradient(135deg, var(--md-sys-color-secondary-fixed), var(--md-sys-color-secondary-fixed-dim));
    color: var(--md-sys-color-on-secondary-fixed-variant);
    box-shadow: 0 4px 12px rgba(var(--md-sys-color-secondary-rgb), 0.3);
  }

  .stat-icon.language {
    background: linear-gradient(135deg, var(--md-sys-color-primary-fixed), var(--md-sys-color-primary-fixed-dim));
    color: var(--md-sys-color-on-primary-fixed-variant);
    box-shadow: 0 4px 12px rgba(var(--md-sys-color-primary-rgb), 0.3);
  }

  .stat-icon.license {
    background: linear-gradient(135deg, var(--md-ref-palette-secondary90), var(--md-ref-palette-tertiary80));
    color: var(--md-sys-color-on-secondary-fixed-variant);
    box-shadow: 0 4px 12px rgba(var(--md-sys-color-secondary-rgb), 0.3);
  }

  .stat-content {
    flex: 1;
    min-width: 0;
  }

  .stat-value {
    font-size: 1rem;
    font-weight: 700;
    color: var(--md-sys-color-on-surface);
    margin-bottom: 0.125rem;
    letter-spacing: 0;
  }

  .stat-label {
    font-size: 0.6875rem;
    color: var(--md-sys-color-on-surface-variant);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    font-weight: 500;
  }

  /* 标签区域 */
  .project-topics {
    background: var(--reay-surface-bg);
    border-radius: 0.9rem;
    padding: 0.75rem;
    margin-bottom: 1rem;
    border: 1px solid var(--reay-surface-border);
    box-shadow: var(--reay-surface-shadow);
    backdrop-filter: blur(var(--reay-glass-blur)) saturate(150%);
    -webkit-backdrop-filter: blur(var(--reay-glass-blur)) saturate(150%);
  }

  .topics-label {
    display: flex;
    align-items: center;
    gap: 0.375rem;
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--md-sys-color-on-surface-variant);
    margin-bottom: 0.5rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .topics-label i {
    font-size: 0.875rem;
    color: var(--md-sys-color-primary);
  }

  .topics-list {
    display: flex;
    flex-wrap: wrap;
    gap: 0.375rem;
  }

  .topic-tag {
    display: inline-flex;
    align-items: center;
    padding: 0.25rem 0.625rem;
    background: var(--reay-chip-bg);
    color: var(--md-sys-color-on-surface-variant);
    border-radius: 999px;
    font-size: 0.75rem;
    font-weight: 500;
    transition: all 0.2s ease;
    cursor: default;
    border: 1px solid var(--reay-chip-border);
  }

  .tag-hash {
    opacity: 0.7;
    margin-right: 0.125rem;
    font-weight: 600;
  }

  .topic-tag:hover {
    background: var(--md-sys-color-primary);
    color: var(--md-sys-color-on-primary);
    transform: translateY(-1px);
    box-shadow: 0 2px 6px rgba(var(--md-sys-color-shadow-rgb), 0.15);
    border-color: var(--md-sys-color-primary);
  }

  /* 操作区域 */
  .actions-section {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
    flex-wrap: wrap;
    padding-top: 0.75rem;
    border-top: 1px solid var(--md-sys-color-outline-variant);
  }

  .action-buttons {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
  }

  .action-button {
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    padding: 0.5rem 1rem;
    background: var(--reay-button-bg);
    color: var(--md-sys-color-on-surface);
    border-radius: 0.85rem;
    text-decoration: none;
    font-size: 0.8125rem;
    font-weight: 600;
    transition: transform 0.15s cubic-bezier(0.4, 0, 0.2, 1),
                box-shadow 0.15s cubic-bezier(0.4, 0, 0.2, 1),
                background-color 0.15s ease;
    border: 1px solid var(--reay-button-border);
    position: relative;
    overflow: hidden;
    cursor: pointer;
    will-change: transform;
  }

  .action-button::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(
      135deg,
      rgba(var(--md-sys-color-primary-rgb), 0.1),
      transparent
    );
    opacity: 0;
    transition: opacity 0.15s ease;
  }

  .action-button:hover::before {
    opacity: 1;
  }

  .action-button.primary {
    background: var(--md-sys-color-primary);
    color: var(--md-sys-color-on-primary);
    border-color: transparent;
  }

  .action-button.primary::before {
    background: linear-gradient(
      135deg,
      rgba(var(--md-sys-color-surface-bright-rgb), 0.2),
      transparent
    );
  }

  .action-button:hover {
    transform: translateY(-1px);
    background: var(--reay-button-bg-hover);
    box-shadow: var(--reay-surface-shadow);
  }

  .action-button:active {
    transform: translateY(0);
    transition-duration: 0.05s;
  }

  .action-button.primary:hover {
    box-shadow: 0 2px 10px rgba(var(--md-sys-color-shadow-rgb), 0.15);
  }

  .action-button i {
    font-size: 0.9375rem;
    transition: transform 0.2s ease;
  }

  .action-button .arrow {
    font-size: 0.875rem;
    margin-left: -0.125rem;
  }

  .action-button:hover .arrow {
    transform: translateX(3px);
  }

  .project-dates {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--md-sys-color-on-surface-variant);
    font-size: 0.75rem;
  }

  .date-item {
    display: flex;
    align-items: center;
    gap: 0.25rem;
  }

  .date-item i {
    font-size: 0.875rem;
    opacity: 0.7;
  }

  .date-separator {
    opacity: 0.5;
    font-weight: 300;
  }

  /* 响应式设计 */
  @media (max-width: 768px) {
    .project-header {
      padding: 1rem;
      border-radius: 10px;
    }

    .title-wrapper {
      gap: 0.75rem;
    }

    .icon-wrapper {
      width: 36px;
      height: 36px;
      font-size: 1.125rem;
    }

    .project-title {
      font-size: 1.25rem;
    }

    .project-subtitle {
      font-size: 0.8125rem;
    }

    .stats-cards {
      grid-template-columns: repeat(2, 1fr);
      gap: 0.5rem;
    }

    .stat-card {
      padding: 0.625rem;
    }

    .stat-icon {
      width: 28px;
      height: 28px;
      font-size: 1rem;
    }

    .stat-value {
      font-size: 0.9375rem;
    }

    .stat-label {
      font-size: 0.625rem;
    }

    .actions-section {
      flex-direction: column;
      align-items: stretch;
      gap: 0.75rem;
      padding-top: 0.75rem;
    }

    .action-buttons {
      width: 100%;
    }

    .action-button {
      flex: 1;
      justify-content: center;
      font-size: 0.75rem;
      padding: 0.5rem 0.75rem;
    }

    .project-dates {
      justify-content: center;
      flex-wrap: wrap;
      font-size: 0.6875rem;
    }
  }

  @media (max-width: 480px) {
    .project-header {
      padding: 1rem;
    }

    .title-wrapper {
      flex-direction: column;
      align-items: center;
      text-align: center;
    }

    .stats-cards {
      grid-template-columns: 1fr;
    }

    .action-buttons {
      flex-direction: column;
    }

    .action-button {
      width: 100%;
    }
  }
</style>
