---
const { wide = false, full = false } = Astro.props as {
  wide?: boolean; full?: boolean;
};
const cls = full
  ? 'w-full'
  : wide
  ? 'mx-auto max-w-1440px px-4 sm:px-6'
  : 'mx-auto max-w-1100px px-4 sm:px-6';
---
<div class={cls}>
  <slot />
</div>