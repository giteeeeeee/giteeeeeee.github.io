---
/**
 * Tags Index Page
 * Display cloud view of all tags
 */
import ArchiveLayout from '../../../layouts/archives/ArchiveLayout.astro';
import TagCloud from '../../../components/archives/TagCloud.astro';
import { getAllTags, getTagUrl } from '../../../utils/blog';
import { useI18n } from '../../../utils/i18n';

// Data layer: Get all tags
const allTags = await getAllTags();
const { t } = useI18n();
---

<ArchiveLayout
  title={t('archives.tags.title')}
  description={t('archives.tags.subtitle')}
  titleKey="archives.tags.title"
  descriptionKey="archives.tags.subtitle"
  showBackButton={true}
  backUrl="/archives"
>
  <!-- Tags statistics -->
  <div class="tags-summary">
    <div class="summary-item reay-card">
      <div class="summary-icon">
        <i class="i-carbon:tag"></i>
      </div>
      <div class="summary-content">
        <span class="summary-label" data-i18n="archives.tags.total">{t('archives.tags.total')}</span>
        <span class="summary-value">{allTags.length}</span>
      </div>
    </div>
    <div class="summary-item reay-card">
      <div class="summary-icon">
        <i class="i-carbon:star-filled"></i>
      </div>
      <div class="summary-content">
        <span class="summary-label" data-i18n="archives.tags.hottest">{t('archives.tags.hottest')}</span>
        <span class="summary-value">{allTags[0]?.tag || '-'}</span>
      </div>
    </div>
    <div class="summary-item reay-card">
      <div class="summary-icon">
        <i class="i-carbon:document"></i>
      </div>
      <div class="summary-content">
        <span class="summary-label" data-i18n="archives.timeline.totalPosts">{t('archives.timeline.totalPosts')}</span>
        <span class="summary-value">
          {allTags.reduce((sum, tag) => sum + tag.count, 0)}
        </span>
      </div>
    </div>
  </div>

  <!-- Tag cloud -->
  {allTags.length > 0 ? (
    <TagCloud tags={allTags} />
  ) : (
    <div class="empty-state">
      <i class="i-carbon:tag-none"></i>
      <h3 data-i18n="archives.empty.tags.title">{t('archives.empty.tags.title')}</h3>
      <p data-i18n="archives.empty.tags.description">{t('archives.empty.tags.description')}</p>
    </div>
  )}

  <!-- Tag list (table view) -->
  {allTags.length > 0 && (
    <section class="tags-table-section">
      <h2 class="section-title">
        <i class="i-carbon:list"></i>
        <span data-i18n="archives.tags.list">{t('archives.tags.list')}</span>
      </h2>
      <div class="tags-table">
        {allTags.map((tag, index) => (
          <a href={getTagUrl(tag.tag)} class="tag-row">
            <div class="tag-rank">#{index + 1}</div>
            <div class="tag-name">
              <i class="i-carbon:tag"></i>
              {tag.tag}
            </div>
            <div class="tag-count-badge">
              {tag.count} <span data-i18n="archives.postsCount">{t('archives.postsCount')}</span>
            </div>
            <div class="tag-arrow">
              <i class="i-carbon:arrow-right"></i>
            </div>
          </a>
        ))}
      </div>
    </section>
  )}
</ArchiveLayout>

<style>
  .tags-summary {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 1.25rem;
    margin-bottom: 2.5rem;
  }

  .summary-item {
    display: flex;
    align-items: center;
    gap: 1.25rem;
    padding: 1.5rem;
    background: var(--reay-surface-bg);
    border: 1px solid var(--reay-surface-border);
    border-radius: 1.1rem;
    box-shadow: var(--reay-surface-shadow);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
  }

  .summary-item::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 4px;
    height: 100%;
    background: linear-gradient(to bottom, var(--md-sys-color-primary), var(--md-sys-color-tertiary));
    transform: scaleY(0);
    transition: transform 0.3s ease;
  }

  .summary-item:hover {
    border-color: var(--reay-surface-border-hover);
    transform: translateY(-4px);
    box-shadow: 
      var(--reay-surface-shadow-hover),
      0 0 0 1px rgba(var(--md-sys-color-primary-rgb), 0.24) inset;
  }

  .summary-item:hover::before {
    transform: scaleY(1);
  }

  .summary-icon {
    flex-shrink: 0;
    width: 56px;
    height: 56px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, var(--md-sys-color-primary-container), var(--md-sys-color-tertiary-container));
    border-radius: 14px;
    font-size: 1.75rem;
    color: var(--md-sys-color-primary);
    transition: all 0.3s ease;
  }

  .summary-item:hover .summary-icon {
    transform: rotate(10deg) scale(1.1);
    box-shadow: 0 4px 16px rgba(var(--md-sys-color-shadow-rgb), 0.15);
  }

  .summary-content {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    flex: 1;
  }

  .summary-label {
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--md-sys-color-on-surface-variant);
    letter-spacing: 0.01em;
  }

  .summary-value {
    font-size: 1.75rem;
    font-weight: 700;
    color: var(--md-sys-color-on-surface);
    line-height: 1;
    background: linear-gradient(135deg, var(--md-sys-color-primary), var(--md-sys-color-tertiary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .empty-state {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 4rem 2rem;
    text-align: center;
  }

  .empty-state i {
    font-size: 4rem;
    color: var(--md-sys-color-outline);
    margin-bottom: 1rem;
  }

  .empty-state h3 {
    font-size: 1.5rem;
    color: var(--md-sys-color-on-surface);
    margin: 0 0 0.5rem 0;
  }

  .empty-state p {
    color: var(--md-sys-color-on-surface-variant);
    margin: 0;
  }

  .tags-table-section {
    margin-top: 3.5rem;
  }

  .section-title {
    display: flex;
    align-items: center;
    gap: 0.875rem;
    font-size: 1.625rem;
    font-weight: 700;
    color: var(--md-sys-color-on-surface);
    margin: 0 0 1.75rem 0;
    padding-bottom: 0.75rem;
    border-bottom: 2px solid var(--md-sys-color-outline-variant);
  }

  .section-title i {
    font-size: 1.5rem;
    padding: 0.5rem;
    background: linear-gradient(135deg, var(--md-sys-color-primary-container), var(--md-sys-color-tertiary-container));
    color: var(--md-sys-color-primary);
    border-radius: 10px;
  }

  .tags-table {
    display: flex;
    flex-direction: column;
    gap: 0.875rem;
  }

  .tag-row {
    display: grid;
    grid-template-columns: auto 1fr auto auto;
    align-items: center;
    gap: 1.25rem;
    padding: 1.125rem 1.5rem;
    background: var(--md-sys-color-surface-container-low);
    border: 2px solid var(--md-sys-color-outline-variant);
    border-left: 4px solid var(--md-sys-color-outline-variant);
    border-radius: 12px;
    text-decoration: none;
    color: inherit;
    transition: all 0.35s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
  }

  .tag-row::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0;
    bottom: 0;
    width: 4px;
    background: linear-gradient(to bottom, var(--md-sys-color-primary), var(--md-sys-color-tertiary));
    transform: scaleY(0);
    transition: transform 0.35s ease;
  }

  .tag-row:hover {
    background: var(--md-sys-color-surface-container);
    border-color: var(--md-sys-color-primary);
    border-left-color: transparent;
    transform: translateX(12px);
    box-shadow: 
      0 6px 20px rgba(var(--md-sys-color-shadow-rgb), 0.12),
      0 0 0 1px var(--md-sys-color-primary) inset;
  }

  .tag-row:hover::before {
    transform: scaleY(1);
  }

  .tag-rank {
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.9375rem;
    font-weight: 700;
    color: var(--md-sys-color-on-surface-variant);
    background: var(--md-sys-color-surface-container);
    border-radius: 10px;
    transition: all 0.3s ease;
  }

  .tag-row:hover .tag-rank {
    background: linear-gradient(135deg, var(--md-sys-color-primary-container), var(--md-sys-color-tertiary-container));
    color: var(--md-sys-color-primary);
    transform: scale(1.1) rotate(-5deg);
    box-shadow: 0 4px 12px rgba(var(--md-sys-color-shadow-rgb), 0.1);
  }

  .tag-name {
    display: flex;
    align-items: center;
    gap: 0.625rem;
    font-size: 1.0625rem;
    font-weight: 600;
    color: var(--md-sys-color-on-surface);
    transition: color 0.3s ease;
  }

  .tag-name i {
    font-size: 1.125rem;
    color: var(--md-sys-color-on-surface-variant);
    transition: all 0.3s ease;
  }

  .tag-row:hover .tag-name {
    color: var(--md-sys-color-primary);
  }

  .tag-row:hover .tag-name i {
    color: var(--md-sys-color-primary);
    transform: rotate(10deg);
  }

  .tag-count-badge {
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--md-sys-color-on-surface-variant);
    padding: 0.5rem 1rem;
    background: var(--md-sys-color-surface-container);
    border-radius: 999px;
    transition: all 0.3s ease;
    white-space: nowrap;
  }

  .tag-row:hover .tag-count-badge {
    background: linear-gradient(135deg, var(--md-sys-color-primary-container), var(--md-sys-color-tertiary-container));
    color: var(--md-sys-color-primary);
    transform: scale(1.05);
  }

  .tag-arrow {
    font-size: 1.25rem;
    color: var(--md-sys-color-primary);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .tag-row:hover .tag-arrow {
    transform: translateX(6px) scale(1.2);
    filter: drop-shadow(0 2px 4px rgba(var(--md-sys-color-shadow-rgb), 0.2));
  }

  @media (max-width: 768px) {
    .tags-summary {
      grid-template-columns: 1fr;
      gap: 1rem;
      margin-bottom: 2rem;
    }

    .summary-item {
      padding: 1.25rem;
      gap: 1rem;
    }

    .summary-icon {
      width: 48px;
      height: 48px;
      font-size: 1.5rem;
      border-radius: 12px;
    }

    .summary-value {
      font-size: 1.5rem;
    }

    .tags-table-section {
      margin-top: 2.5rem;
    }

    .section-title {
      font-size: 1.375rem;
      gap: 0.625rem;
      margin-bottom: 1.25rem;
      padding-bottom: 0.625rem;
    }

    .section-title i {
      font-size: 1.25rem;
      padding: 0.375rem;
    }

    .tags-table {
      gap: 0.75rem;
    }

    .tag-row {
      grid-template-columns: auto 1fr auto;
      gap: 0.875rem;
      padding: 1rem 1.125rem;
      border-left-width: 3px;
    }

    .tag-row:hover {
      transform: translateX(8px);
    }

    .tag-rank {
      width: 36px;
      height: 36px;
      font-size: 0.875rem;
      border-radius: 8px;
    }

    .tag-name {
      font-size: 0.9375rem;
      gap: 0.5rem;
    }

    .tag-name i {
      font-size: 1rem;
    }

    .tag-count-badge {
      display: none;
    }

    .tag-arrow {
      font-size: 1.125rem;
    }

    .tag-row:hover .tag-arrow {
      transform: translateX(4px) scale(1.15);
    }
  }
</style>
